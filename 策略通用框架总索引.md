# 聚宽558策略通用框架总索引

> **文档说明**: 本文档是558+个聚宽策略的通用框架总索引，将全部策略归纳为10大类型，每种类型都有独立的通用框架设计文档。
> **更新日期**: 2026-04-20

---

## 一、框架总览

| 序号 | 策略类型 | 文件数量 | 框架文档 | 核心模块数 | 配置项数 |
|------|---------|---------|----------|-----------|---------|
| 1 | 小市值策略 | ~120 | [小市值策略通用框架设计.md](./小市值策略通用框架设计.md) | 8 | 50+ |
| 2 | ETF轮动策略 | ~75 | [ETF轮动策略通用框架设计.md](./ETF轮动策略通用框架设计.md) | 7 | 30+ |
| 3 | 多因子策略 | ~55 | [多因子策略通用框架设计.md](./多因子策略通用框架设计.md) | 6 | 40+ |
| 4 | 短线战法 | ~50 | [短线战法通用框架设计.md](./短线战法通用框架设计.md) | 5 | 35+ |
| 5 | 价值/成长策略 | ~45 | (见下方框架定义) | 5 | 60+ |
| 6 | 机器学习策略 | ~40 | [机器学习策略通用框架设计.md](./机器学习策略通用框架设计.md) | 5 | 40+ |
| 7 | 高股息策略 | ~30 | (见下方框架定义) | 3 | 45+ |
| 8 | 动量/反转策略 | ~20 | [动量反转策略通用框架设计.md](./动量反转策略通用框架设计.md) | 3 | 25+ |
| 9 | 风控/择时策略 | ~25 | [风控择时策略通用框架设计.md](./风控择时策略通用框架设计.md) | 8 | 35+ |
| 10 | 综合策略(CTA/可转债/FOF) | ~37 | [综合策略框架_CTA_可转债_基金FOF.md](./综合策略框架_CTA_可转债_基金FOF.md) | 9 | 40+ |

---

## 二、通用策略基类设计

所有策略类型都继承自统一的策略基类，确保新策略可以无缝接入框架。

```python
from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from typing import Dict, List, Optional, Tuple
from datetime import datetime, timedelta

@dataclass
class BaseStrategyConfig:
    """所有策略配置的基类"""
    # 股票池配置
    stock_pool: str = "all_a"
    exclude_st: bool = True
    exclude_kcb: bool = True
    exclude_bse: bool = True
    new_stock_days: int = 375
    
    # 调仓配置
    rebalance_frequency: str = "weekly"
    rebalance_day: int = 1
    rebalance_time: str = "09:30"
    
    # 仓位配置
    position_mode: str = "equal"
    
    # 风控配置
    hold_limit_up: bool = True
    blacklist_days: int = 5


class BaseStrategy(ABC):
    """
    策略基类 - 所有策略类型的统一入口
    
    子类必须实现:
    - get_target_stocks(): 获取目标股票列表
    """
    
    def __init__(self, config: BaseStrategyConfig):
        self.config = config
        self.hold_list = []
        self.target_list = []
        self.high_limit_list = []
        
    @abstractmethod
    def get_target_stocks(self, context) -> List[str]:
        """子类实现: 返回目标股票列表"""
        pass
    
    def initialize(self, context):
        """初始化 - 所有策略共用"""
        self._setup_environment(context)
        self._schedule_tasks()
        
    def before_trading(self, context):
        """盘前准备 - 所有策略共用"""
        self._update_hold_list(context)
        self._update_limit_up_list(context)
        
    def rebalance(self, context):
        """调仓主流程 - 所有策略共用"""
        # 1. 获取目标股票
        self.target_list = self.get_target_stocks(context)
        
        # 2. 卖出不在目标列表的股票
        self._sell(context)
        
        # 3. 买入目标股票
        self._buy(context)
        
    def _setup_environment(self, context):
        """环境设置"""
        set_option('use_real_price', True)
        set_option('avoid_future_data', True)
        log.set_level('order', 'error')
        
    def _schedule_tasks(self):
        """定时任务调度"""
        run_daily(self.before_trading, '9:05')
        
        if self.config.rebalance_frequency == 'weekly':
            run_weekly(self.rebalance, self.config.rebalance_day, self.config.rebalance_time)
        elif self.config.rebalance_frequency == 'monthly':
            run_monthly(self.rebalance, self.config.rebalance_day, self.config.rebalance_time)
        elif self.config.rebalance_frequency == 'daily':
            run_daily(self.rebalance, self.config.rebalance_time)
            
        run_daily(self.check_limit_up, '14:00')
        
    def _update_hold_list(self, context):
        self.hold_list = list(context.portfolio.positions.keys())
        
    def _update_limit_up_list(self, context):
        if self.hold_list:
            df = get_price(self.hold_list, end_date=context.previous_date,
                          frequency='daily', fields=['close', 'high_limit'], count=1)
            df = df[df['close'] == df['high_limit']]
            self.high_limit_list = list(df.code)
        else:
            self.high_limit_list = []
            
    def _sell(self, context):
        """卖出逻辑"""
        for stock in self.hold_list:
            if stock not in self.target_list and stock not in self.high_limit_list:
                order_target_value(stock, 0)
                
    def _buy(self, context):
        """买入逻辑"""
        target_list = self.target_list[:getattr(self.config, 'stock_num', 10)]
        position_count = len(context.portfolio.positions)
        target_num = len(target_list)
        
        if target_num > position_count:
            value = context.portfolio.cash / (target_num - position_count)
            for stock in target_list:
                if context.portfolio.positions[stock].total_amount == 0:
                    order_target_value(stock, value)
                    if len(context.portfolio.positions) == target_num:
                        break
                        
    def check_limit_up(self, context):
        """涨停监控"""
        if not self.high_limit_list:
            return
        current_data = get_current_data()
        for stock in self.high_limit_list:
            if current_data[stock].last_price < current_data[stock].high_limit:
                order_target_value(stock, 0)
```

---

## 三、各类型框架核心差异

### 3.1 小市值策略
**核心差异**: 因子评分 + 市值升序排序
```python
class SmallCapStrategy(BaseStrategy):
    def get_target_stocks(self, context):
        # 1. 股票池过滤
        stocks = self.pool_builder.build(context.previous_date)
        # 2. 因子计算 + 评分
        scores = self.scorer.score(stocks, context.previous_date)
        # 3. 按流通市值升序取最小
        return self.selector.select_by_market_cap(scores, ascending=True)
```

### 3.2 ETF轮动策略
**核心差异**: ETF池 + 动量计算 + 择时信号
```python
class ETFRotationStrategy(BaseStrategy):
    def get_target_stocks(self, context):
        # 1. ETF候选池
        etfs = self.pool_manager.get_pool()
        # 2. 动量计算
        momentum = self.momentum_calc.calculate(etfs, context.previous_date)
        # 3. 择时信号
        signal = self.timer.generate_signal(context.previous_date)
        # 4. 排序选股
        return self.ranker.rank(momentum, signal)
```

### 3.3 多因子策略
**核心差异**: 因子库 + 因子处理 + 因子合成
```python
class MultiFactorStrategy(BaseStrategy):
    def get_target_stocks(self, context):
        # 1. 因子获取
        factors = self.factor_lib.get_factors(context.previous_date)
        # 2. 因子处理(去极值/标准化/中性化)
        factors = self.processor.process(factors)
        # 3. 因子合成
        scores = self.combiner.combine(factors)
        # 4. Top N选股
        return self.portfolio.top_n(scores, self.config.stock_num)
```

### 3.4 短线战法
**核心差异**: 涨停识别 + 形态判断 + 日内交易
```python
class ShortTermStrategy(BaseStrategy):
    def get_target_stocks(self, context):
        # 1. 昨日涨停股
        hl_stocks = self.detector.get_limit_up(context.previous_date)
        # 2. 形态识别(低开/回调/反包)
        candidates = self.pattern_recognizer.filter(hl_stocks)
        # 3. 买入信号
        return self.buy_signal_gen.generate(candidates)
```

### 3.5 价值/成长策略
**核心差异**: 基本面筛选 + 估值排序
```python
class ValueGrowthStrategy(BaseStrategy):
    def get_target_stocks(self, context):
        # 1. 基本面数据
        data = self.data_provider.get_all(context.previous_date)
        # 2. 价值筛选(PE/PB/股息率)
        stocks = self.value_screener.screen(data)
        # 3. 成长筛选(营收增长/利润增长)
        stocks = self.growth_screener.screen(stocks)
        # 4. 质量筛选(ROE/ROA/FCF)
        stocks = self.quality_screener.screen(stocks)
        # 5. 排序(PE升序/市值降序)
        return self._sort(stocks)
```

### 3.6 机器学习策略
**核心差异**: 特征工程 + 模型训练 + 预测选股
```python
class MLStrategy(BaseStrategy):
    def get_target_stocks(self, context):
        # 1. 特征矩阵
        X = self.feature_engineer.build(context.previous_date)
        # 2. 模型训练/加载
        model = self.model_manager.load_or_train()
        # 3. 预测
        predictions = model.predict(X)
        # 4. 按预测值排序
        return self.selector.select_by_prediction(predictions)
```

### 3.7 高股息策略
**核心差异**: 分红数据 + 股息率计算 + 质量过滤
```python
class HighDividendStrategy(BaseStrategy):
    def get_target_stocks(self, context):
        # 1. 分红数据
        dividends = self.dividend_provider.get(context.previous_date)
        # 2. 股息率计算
        yields = self.yield_calc.calculate(dividends)
        # 3. 股息率筛选
        stocks = self.yield_screener.screen(yields)
        # 4. 质量筛选(PE/PEG/增长)
        stocks = self.quality_screener.screen(stocks)
        # 5. 排序(市值升序)
        return self._sort(stocks)
```

### 3.8 动量/反转策略
**核心差异**: 动量计算 + 反转计算 + 组合得分
```python
class MomentumReversalStrategy(BaseStrategy):
    def get_target_stocks(self, context):
        # 1. 动量计算
        momentum = self.momentum_calc.calculate(context.previous_date)
        # 2. 反转计算
        reversal = self.reversal_calc.calculate(context.previous_date)
        # 3. 组合得分
        scores = self.combiner.combine(momentum, reversal)
        # 4. 排序选股
        return self._sort(scores)
```

### 3.9 风控/择时策略
**核心差异**: 择时信号 + 仓位控制 + 股票过滤
```python
class RiskTimingModule:
    def get_timing_signal(self, context) -> int:
        # RSRS/MA/市场宽度/拥挤率
        pass
    
    def get_position_ratio(self, context) -> float:
        # 根据择时信号返回仓位比例
        pass
    
    def filter_stocks(self, stock_list, context) -> List[str]:
        # ST/停牌/次新/科创/涨跌停
        pass
```

### 3.10 综合策略(CTA/可转债/FOF)
**核心差异**: 多品种 + 特殊交易规则
```python
# CTA: 期货信号 + 波动率仓位
class CTAStrategy:
    def get_signal(self, context) -> int:
        # ATR/趋势/突破
        pass

# 可转债: 双低筛选
class ConvertibleBondStrategy:
    def screen_bonds(self, context) -> List[str]:
        # 价格 + 溢价率
        pass

# FOF: 资产配置
class FundFOFStrategy:
    def allocate(self, context) -> Dict[str, float]:
        # 波动率平价/风险平价
        pass
```

---

## 四、新策略接入指南

### 4.1 接入步骤

```
1. 确定策略类型 → 选择对应框架
2. 继承BaseStrategy → 实现get_target_stocks()
3. 配置Config类 → 设置参数
4. 注册定时任务 → 调仓频率
5. 回测验证 → 参数优化
```

### 4.2 示例: 新小市值策略接入

```python
from small_cap_framework import SmallCapStrategy, SmallCapConfig

# 1. 定义配置
config = SmallCapConfig(
    factors=['ARBR', 'SGAI', 'sales_growth'],
    factor_weights=[-2.34, -694.79, 0.029],
    stock_num=10,
    rebalance_frequency='weekly',
    rebalance_day=1,
)

# 2. 创建策略实例
strategy = SmallCapStrategy(config)

# 3. 在聚宽平台注册
def initialize(context):
    strategy.initialize(context)
```

---

## 五、框架文件清单

| 文件 | 内容 |
|------|------|
| [小市值策略通用框架设计.md](./小市值策略通用框架设计.md) | 8模块/50+配置/3映射示例 |
| [ETF轮动策略通用框架设计.md](./ETF轮动策略通用框架设计.md) | 7模块/30+配置/10映射示例 |
| [多因子策略通用框架设计.md](./多因子策略通用框架设计.md) | 6模块/40+配置/12变体 |
| [短线战法通用框架设计.md](./短线战法通用框架设计.md) | 5模块/35+配置/6映射示例 |
| [机器学习策略通用框架设计.md](./机器学习策略通用框架设计.md) | 5模块/40+配置/5映射示例 |
| [动量反转策略通用框架设计.md](./动量反转策略通用框架设计.md) | 3模块/25+配置/8映射示例 |
| [风控择时策略通用框架设计.md](./风控择时策略通用框架设计.md) | 8模块/35+配置/8映射示例 |
| [综合策略框架_CTA_可转债_基金FOF.md](./综合策略框架_CTA_可转债_基金FOF.md) | 9模块/40+配置/15映射示例 |
| [价值/成长策略框架](#三5-价值成长策略) | 5模块/60+配置/7映射示例(本文档内定义) |
| [高股息策略框架](#三7-高股息策略) | 3模块/45+配置/7映射示例(本文档内定义) |

---

## 六、共享模块库

以下模块被多个策略类型共享使用：

| 模块 | 使用策略类型 | 功能 |
|------|-------------|------|
| StockFilter | 全部 | ST/停牌/次新/科创过滤 |
| PositionManager | 全部 | 等权/市值/波动率加权 |
| TradeExecutor | 全部 | 买卖执行/涨停处理 |
| RSRSTimer | ETF/多因子/风控 | RSRS择时 |
| MATimer | 小市值/ETF/风控 | 均线择时 |
| FactorLibrary | 小市值/多因子/ML | 因子获取 |
| FactorProcessor | 多因子/ML | 去极值/标准化/中性化 |
| MomentumCalculator | ETF/动量 | 动量计算 |
| DividendDataProvider | 高股息/价值 | 分红数据 |

---

*文档生成日期: 2026-04-20*
*策略来源: 聚宽社区 (JoinQuant)*
