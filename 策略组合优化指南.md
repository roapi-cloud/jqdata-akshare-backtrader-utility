# 策略组合优化指南

## 目录
1. [判断策略相关度](#1-判断策略相关度)
2. [策略叠加方法](#2-策略叠加方法)
3. [组合优化](#3-组合优化)
4. [动态相关性分析](#4-动态相关性分析)
5. [实践建议](#5-实践建议)

---

## 1. 判断策略相关度

### 核心问题
- 相关性必须通过回测才能得出真实数据
- 相关性是动态变化的，不同市场环境下可能完全不同
- 相关性随时间变化是正常现象

### 基础相关性计算

```python
import pandas as pd
import numpy as np

def calc_strategy_correlation(nav1, nav2, window=60):
    """
    计算两个策略净值序列的相关度
    nav1, nav2: 策略净值序列 (pd.Series)
    window: 滚动窗口天数
    """
    # 计算日收益率
    ret1 = nav1.pct_change().dropna()
    ret2 = nav2.pct_change().dropna()
    
    # 对齐时间索引
    common_idx = ret1.index.intersection(ret2.index)
    ret1, ret2 = ret1.loc[common_idx], ret2.loc[common_idx]
    
    # 整体相关系数
    overall_corr = ret1.corr(ret2)
    
    # 滚动相关系数（观察相关性是否稳定）
    rolling_corr = ret1.rolling(window).corr(ret2)
    
    return {
        'overall_corr': overall_corr,
        'rolling_corr_mean': rolling_corr.mean(),
        'rolling_corr_std': rolling_corr.std()
    }
```

### 相关性判断标准

| 相关系数范围 | 判断 | 组合建议 |
|-------------|------|----------|
| \|ρ\| < 0.3 | 低相关 | 非常适合组合 |
| 0.3 ≤ \|ρ\| < 0.5 | 中等相关 | 可以组合 |
| 0.5 ≤ \|ρ\| < 0.7 | 较高相关 | 谨慎组合 |
| \|ρ\| ≥ 0.7 | 高相关 | 不建议组合 |

---

## 2. 策略叠加方法

### 方法一：等权叠加

最简单的方法，每个策略分配相同权重。

```python
def equal_weight_combine(nav_list):
    """
    等权组合多个策略
    nav_list: 策略净值序列列表
    """
    df = pd.concat(nav_list, axis=1).dropna()
    combined_nav = df.mean(axis=1)
    return combined_nav / combined_nav.iloc[0]
```

### 方法二：风险平价叠加

波动率低的策略分配更多权重，实现风险均衡。

```python
def risk_parity_combine(nav_list):
    """
    风险平价：波动率低的策略分配更多权重
    """
    rets = pd.concat([nav.pct_change() for nav in nav_list], axis=1).dropna()
    vols = rets.std()
    weights = (1 / vols) / (1 / vols).sum()
    combined_ret = (rets * weights).sum(axis=1)
    return (1 + combined_ret).cumprod()
```

### 方法三：动量加权叠加

近期表现好的策略分配更多权重。

```python
def momentum_weight_combine(nav_list, lookback=20):
    """
    动量加权：近期表现好的策略分配更多权重
    lookback: 回看窗口天数
    """
    rets = pd.concat([nav.pct_change() for nav in nav_list], axis=1).dropna()
    momentum = rets.rolling(lookback).mean()
    # 动量为负的设为0
    momentum = momentum.clip(lower=0)
    weights = momentum.div(momentum.sum(axis=1), axis=0)
    combined_ret = (rets * weights.shift(1)).sum(axis=1)
    return (1 + combined_ret).cumprod()
```

### 方法对比

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 等权 | 简单稳定 | 不考虑策略差异 | 策略表现相近时 |
| 风险平价 | 控制波动 | 可能错过高收益 | 波动率差异大时 |
| 动量加权 | 跟踪趋势 | 需要频繁调整 | 趋势明确时 |

---

## 3. 组合优化

### 最大化夏普比率优化

```python
from scipy.optimize import minimize

def optimize_portfolio(nav_list, risk_free_rate=0.03/252):
    """
    优化策略组合权重
    目标：最大化夏普比率
    
    nav_list: 策略净值序列列表
    risk_free_rate: 无风险利率（日化）
    """
    rets = pd.concat([nav.pct_change() for nav in nav_list], axis=1).dropna()
    n = len(nav_list)
    
    def neg_sharpe(weights):
        port_ret = (rets * weights).sum(axis=1)
        mean_ret = port_ret.mean() - risk_free_rate
        vol = port_ret.std()
        return -mean_ret / vol if vol > 0 else 0
    
    # 约束：权重和为1
    constraints = {'type': 'eq', 'fun': lambda w: w.sum() - 1}
    # 边界：每个权重在[0, 1]之间
    bounds = [(0, 1)] * n
    
    # 初始等权
    x0 = np.ones(n) / n
    
    result = minimize(neg_sharpe, x0, method='SLSQP', 
                     bounds=bounds, constraints=constraints)
    
    return result.x
```

### 带约束的优化

```python
def optimize_portfolio_with_constraints(nav_list, max_weight=0.4, min_weight=0.1):
    """
    带权重约束的组合优化
    max_weight: 单策略最大权重
    min_weight: 单策略最小权重
    """
    rets = pd.concat([nav.pct_change() for nav in nav_list], axis=1).dropna()
    n = len(nav_list)
    
    # 计算协方差矩阵
    cov_matrix = rets.cov() * 252  # 年化
    mean_rets = rets.mean() * 252  # 年化
    
    def portfolio_volatility(weights):
        return np.sqrt(weights.T @ cov_matrix @ weights)
    
    def neg_sharpe(weights):
        port_ret = weights @ mean_rets
        port_vol = portfolio_volatility(weights)
        return -port_ret / port_vol if port_vol > 0 else 0
    
    constraints = {'type': 'eq', 'fun': lambda w: w.sum() - 1}
    bounds = [(min_weight, max_weight)] * n
    x0 = np.ones(n) / n
    
    result = minimize(neg_sharpe, x0, method='SLSQP',
                     bounds=bounds, constraints=constraints)
    
    return result.x
```

### 组合评估指标

```python
def evaluate_portfolio(nav):
    """
    评估组合策略表现
    """
    ret = nav.pct_change().dropna()
    
    # 年化收益
    ann_ret = (nav.iloc[-1] / nav.iloc[0]) ** (252 / len(nav)) - 1
    
    # 年化波动率
    ann_vol = ret.std() * np.sqrt(252)
    
    # 夏普比率
    sharpe = ann_ret / ann_vol if ann_vol > 0 else 0
    
    # 最大回撤
    cummax = nav.cummax()
    drawdown = (nav - cummax) / cummax
    max_dd = drawdown.min()
    
    # Calmar比率（年化收益/最大回撤）
    calmar = ann_ret / abs(max_dd) if max_dd != 0 else 0
    
    # 胜率
    win_rate = (ret > 0).mean()
    
    return {
        '年化收益': f'{ann_ret:.2%}',
        '年化波动': f'{ann_vol:.2%}',
        '夏普比率': f'{sharpe:.2f}',
        '最大回撤': f'{max_dd:.2%}',
        'Calmar比率': f'{calmar:.2f}',
        '胜率': f'{win_rate:.2%}'
    }
```

---

## 4. 动态相关性分析

### 为什么需要分析动态相关性

- 相关性不是固定的，会随市场环境变化
- 牛市、熊市、震荡市的相关性可能完全不同
- 组合的价值在于危机时能分散风险

### 动态相关性分析函数

```python
import matplotlib.pyplot as plt

def analyze_dynamic_correlation(nav1, nav2, name1='策略A', name2='策略B'):
    """
    分析两个策略的动态相关性
    """
    ret1 = nav1.pct_change().dropna()
    ret2 = nav2.pct_change().dropna()
    
    common_idx = ret1.index.intersection(ret2.index)
    ret1, ret2 = ret1.loc[common_idx], ret2.loc[common_idx]
    
    # 滚动相关系数（20日、60日、120日）
    corr_20 = ret1.rolling(20).corr(ret2)
    corr_60 = ret1.rolling(60).corr(ret2)
    corr_120 = ret1.rolling(120).corr(ret2)
    
    # 年度相关性
    yearly_corr = ret1.resample('Y').corr(ret2)
    
    # 统计指标
    stats = {
        '整体相关系数': ret1.corr(ret2),
        '60日滚动相关均值': corr_60.mean(),
        '60日滚动相关标准差': corr_60.std(),
        '相关系数>0.5的比例': (corr_60 > 0.5).mean(),
        '相关系数<-0.3的比例': (corr_60 < -0.3).mean(),
        '最大滚动相关': corr_60.max(),
        '最小滚动相关': corr_60.min(),
    }
    
    # 可视化
    fig, axes = plt.subplots(3, 1, figsize=(12, 10))
    
    # 净值曲线
    ax1 = axes[0]
    ax1.plot(nav1 / nav1.iloc[0], label=name1)
    ax1.plot(nav2 / nav2.iloc[0], label=name2)
    ax1.set_title('策略净值对比')
    ax1.legend()
    
    # 滚动相关系数
    ax2 = axes[1]
    ax2.plot(corr_20, label='20日滚动相关', alpha=0.5)
    ax2.plot(corr_60, label='60日滚动相关', linewidth=2)
    ax2.plot(corr_120, label='120日滚动相关', linewidth=2)
    ax2.axhline(y=0.3, color='r', linestyle='--', label='高相关阈值')
    ax2.axhline(y=-0.3, color='g', linestyle='--', label='负相关阈值')
    ax2.axhline(y=0, color='gray', linestyle='-')
    ax2.set_ylim(-1, 1)
    ax2.set_title('滚动相关系数')
    ax2.legend()
    
    # 年度相关性
    ax3 = axes[2]
    if not yearly_corr.empty:
        yearly_corr.plot(kind='bar', ax=ax3)
    ax3.set_title('年度相关系数')
    ax3.axhline(y=0.3, color='r', linestyle='--')
    
    plt.tight_layout()
    plt.show()
    
    return stats
```

### 相关性不稳定的原因（正常现象）

| 市场环境 | 相关性表现 | 原因 |
|---------|-----------|------|
| 牛市 | 可能较低 | 不同风格轮动 |
| 熊市 | 可能升高 | 恐慌性抛售，同涨同跌 |
| 震荡市 | 波动较大 | 市场结构变化 |

### 危机相关性检验

**核心原则：组合的价值在于危机时能分散风险**

```python
def is_good_for_combination(nav1, nav2, threshold=0.5):
    """
    判断两个策略是否适合组合
    核心：极端行情下相关性是否飙升
    """
    ret1 = nav1.pct_change().dropna()
    ret2 = nav2.pct_change().dropna()
    
    common_idx = ret1.index.intersection(ret2.index)
    ret1, ret2 = ret1.loc[common_idx], ret2.loc[common_idx]
    
    # 计算市场大跌时的相关性（日跌幅>2%的交易日）
    market_down = ret1 < -0.02
    crisis_corr = ret1[market_down].corr(ret2[market_down])
    
    # 计算正常时期的相关性
    normal_corr = ret1[~market_down].corr(ret2[~market_down])
    
    # 滚动相关标准差（稳定性）
    rolling_corr = ret1.rolling(60).corr(ret2)
    corr_std = rolling_corr.std()
    
    result = {
        '正常时期相关性': normal_corr,
        '大跌时期相关性': crisis_corr,
        '相关性标准差': corr_std,
        '适合组合': crisis_corr < threshold and corr_std < 0.3
    }
    
    return result
```

---

## 5. 实践建议

### 选择策略的原则

1. **优先选择低相关策略**
   - 整体相关系数 < 0.3
   - 危机时期相关系数 < 0.5

2. **覆盖不同市场环境**
   - 至少包含一轮牛熊周期（3-5年）
   - 观察不同市场环境下的表现

3. **考虑策略逻辑差异**
   - 不同选股逻辑（价值/成长/动量）
   - 不同持仓周期（短线/中线/长线）
   - 不同市场（A股/港股/美股）

### 组合管理要点

1. **定期再平衡**
   - 月度或季度重新优化权重
   - 根据最新相关性调整

2. **控制单策略权重上限**
   - 建议单策略不超过40%
   - 避免过度集中

3. **监控组合表现**
   - 跟踪组合夏普比率变化
   - 关注最大回撤是否扩大

4. **考虑策略容量**
   - 大资金优先选择容量大的策略
   - 避免因规模影响策略效果

### 常见陷阱

| 陷阱 | 说明 | 解决方案 |
|------|------|----------|
| 过度拟合 | 在历史数据上优化过度 | 使用样本外测试 |
| 忽略交易成本 | 未考虑换手率影响 | 加入滑点和手续费 |
| 相关性突变 | 极端行情下相关性飙升 | 分析危机相关性 |
| 样本不足 | 回测时间太短 | 至少3-5年数据 |

---

## 附录：完整工作流程

```
1. 策略回测
   ↓
2. 计算收益序列
   ↓
3. 相关性分析（静态 + 动态）
   ↓
4. 筛选低相关策略
   ↓
5. 组合优化（确定权重）
   ↓
6. 样本外测试
   ↓
7. 实盘跟踪与再平衡
```
