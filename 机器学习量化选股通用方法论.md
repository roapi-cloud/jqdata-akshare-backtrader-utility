# 量化机器学习通用方法论设计文档

> 基于聚宽平台 10+ 个 ML 策略笔记本的系统性总结

---

## 目录

- [1. 总体架构](#1-总体架构)
- [2. 数据流水线](#2-数据流水线)
- [3. 因子预处理规范](#3-因子预处理规范)
- [4. 标签构建方法](#4-标签构建方法)
- [5. 特征工程体系](#5-特征工程体系)
- [6. 模型选择指南](#6-模型选择指南)
- [7. 训练策略](#7-训练策略)
- [8. 预测与组合构建](#8-预测与组合构建)
- [9. 回测评估体系](#9-回测评估体系)
- [10. 通用代码模板](#10-通用代码模板)
- [11. 关键注意事项](#11-关键注意事项)
- [12. 进阶方法](#12-进阶方法)

---

## 1. 总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      ML 量化选股流水线                            │
├─────────┬──────────┬──────────┬──────────┬──────────┬───────────┤
│ 股票池   │ 因子获取  │ 预处理   │ 标签构建  │ 特征工程  │ 模型训练  │
│ 筛选    │ & 计算   │ & 中性化  │ & 打标签  │ & 选择   │ & 调参   │
├─────────┴──────────┴──────────┴──────────┴──────────┴───────────┤
│                      预测选股 → 组合构建 → 回测评估                │
└─────────────────────────────────────────────────────────────────┘
```

### 1.1 核心设计原则

| 原则 | 说明 |
|------|------|
| **横截面思维** | 每个调仓日独立计算因子、训练/预测、选股 |
| **时间序列划分** | 训练集/测试集严格按时间先后划分，禁止随机 split |
| **防止未来函数** | 因子必须当期可获取，标签必须来自未来 |
| **滚动更新** | 模型定期重训以适应市场风格变化 |

---

## 2. 数据流水线

### 2.1 股票池筛选

```python
def get_stock_universe(date, index='399905.XSHE', list_days=90):
    """获取过滤后的股票池"""
    stocks = get_index_stocks(index, date=date)
    stocks = remove_new(stocks, date, list_days)    # 剔除次新股
    stocks = remove_st(stocks, date)                 # 剔除 ST
    stocks = remove_paused(stocks, date)             # 剔除停牌
    stocks = remove_limit(stocks, date)              # 剔除涨跌停
    return stocks
```

**常用股票池**:
| 指数 | 代码 | 特点 |
|------|------|------|
| 中证500 | 399905.XSHE | 中小盘，最常用 |
| 中证全指 | 000985.XSHG | 覆盖广 |
| 沪深300 | 000300.XSHG | 大盘蓝筹 |

### 2.2 因子来源

| 类别 | 来源 | 示例因子 |
|------|------|----------|
| **估值因子** | 财务数据 | EP, BP, SP, CFP (倒数 PE/PB/PS/PCF) |
| **杠杆因子** | 资产负债表 | 财务杠杆, 产权比率, 现金比率, 流动比率 |
| **质量因子** | 聚宽因子库 | ROE, ROA, GPM, NI, 资产周转率 |
| **成长因子** | 财务指标 | 营收增长率, 净利润增长率 |
| **技术因子** | 技术分析库 | RSI, MACD(DIF/DEA), BIAS, PSY, ATR, VOL |
| **风险因子** | 统计计算 | Skewness20/60, 波动率(Parkinson/GK/YZ) |
| **Barra风格** | 自定义 | 非线性市值, Beta, 动量, 流动性 |
| **量价因子** | 行情数据 | 换手率, 涨跌幅, 量比, K线角度 |

### 2.3 调仓频率

| 频率 | 适用场景 | 优点 | 缺点 |
|------|----------|------|------|
| **月度** | 因子选股(最常用) | 换手低, 财务数据匹配 | 反应慢 |
| **周度** | 技术因子 | 反应较快 | 换手高 |
| **日度** | 高频/强化学习 | 反应最快 | 成本极高 |

---

## 3. 因子预处理规范

> **必须按顺序执行**: 去极值 → 缺失值填充 → 中性化 → 标准化

### 3.1 去极值 (Winsorization)

```python
def winsorize_med(df, scale=5):
    """中位数去极值法"""
    median = df.median()
    mad = (df - median).abs().median()
    upper = median + scale * mad
    lower = median - scale * mad
    return df.clip(lower, upper, axis=1)
```

| 方法 | 参数 | 适用场景 |
|------|------|----------|
| 中位数法 (MAD) | scale=3~5 | **最常用**, 稳健 |
| 3σ 法 | scale=3 | 正态分布数据 |
| 百分位法 | 1%~99% | 简单粗暴 |

### 3.2 缺失值填充

```python
def fill_by_industry(df, date, industry='sw_l1'):
    """行业均值填充"""
    industries = get_industry(df.index, date)
    for factor in df.columns:
        group_mean = df.groupby(industries)[factor].transform('mean')
        df[factor] = df[factor].fillna(group_mean)
    # 无行业数据的用列均值兜底
    return df.fillna(df.mean())
```

### 3.3 中性化 (核心步骤)

```python
def neutralize(df, neutralize_by=['industry', 'market_cap'], date):
    """行业 + 市值中性化"""
    # 1. 行业虚拟变量
    industries = get_industry(df.index, date)
    industry_dummies = pd.get_dummies(industries)
    
    # 2. 对数市值
    ln_mv = np.log(df['market_cap'])
    
    # 3. 横截面回归剔除
    for factor in df.columns:
        X = sm.add_constant(pd.concat([industry_dummies, ln_mv], axis=1))
        y = df[factor]
        valid = y.notnull() & X.notnull().all(axis=1)
        if valid.sum() > 10:
            res = sm.OLS(y[valid], X[valid]).fit()
            df[factor] = res.resid  # 残差 = 剔除行业/市值影响后的纯因子
    return df
```

**为什么要中性化?**
- 不同行业天然有估值差异 (银行PE低、科技PE高)
- 市值大小影响因子表现 (小盘股波动大)
- 中性化后得到的是**行业/市值无关的纯因子暴露**

### 3.4 标准化

```python
def standardize(df):
    """Z-Score 标准化"""
    return (df - df.mean()) / df.std()
```

---

## 4. 标签构建方法

### 4.1 分类标签 (最常用)

```python
def label_top_bottom(returns, top_pct=0.3, bottom_pct=0.3):
    """横截面排名法: 前30%为1, 后30%为0, 中间剔除"""
    returns = returns.sort_values(ascending=False)
    n = len(returns)
    n_top = int(n * top_pct)
    n_bottom = int(n * bottom_pct)
    
    labels = pd.Series(np.nan, index=returns.index)
    labels.iloc[:n_top] = 1      # 赢家
    labels.iloc[-n_bottom:] = 0  # 输家
    # 中间40% 作为噪声剔除
    return labels.dropna()
```

**为什么剔除中间样本?**
- 中间股票涨跌方向不明确, 是噪声
- 剔除后分类边界更清晰, 模型更容易学习

### 4.2 标签类型对比

| 类型 | 定义 | 适用模型 | 优点 | 缺点 |
|------|------|----------|------|------|
| **二分类** | Top30%=1, Bottom30%=0 | 所有分类器 | 边界清晰 | 丢弃中间信息 |
| **三分类** | Top=1, Mid=0, Bottom=-1 | 分类器 | 保留中间 | 中间噪声大 |
| **回归** | 实际收益率 | 回归器 | 信息完整 | 噪声大, 难拟合 |
| **排序** | 横截面排名分位数 | LambdaMART等 | 直接优化排序 | 实现复杂 |
| **RL奖励** | 持仓收益 | DQN/PPO | 端到端优化 | 训练不稳定 |

### 4.3 持有期选择

| 持有期 | 适用场景 | 标签计算 |
|--------|----------|----------|
| **1个月** | 月度调仓(最常用) | `close(t+20) / close(t) - 1` |
| **5天** | 周度调仓 | `close(t+5) / close(t) - 1` |
| **2天** | 短期技术面 | `close(t+2) / close(t) - 1` |

---

## 5. 特征工程体系

### 5.1 特征选择方法

| 方法类别 | 算法 | 工具 | 适用场景 |
|----------|------|------|----------|
| **过滤法** | F检验 (ANOVA) | `SelectKBest(f_classif)` | 快速筛选, 假设线性 |
| | 互信息 | `mutual_info_classif` | 捕捉非线性关系 |
| | 相关系数 | Pearson/Spearman | 简单直观 |
| **嵌入法** | RF特征重要性 | `SelectFromModel(RF)` | **最常用**, 考虑交互 |
| | LightGBM重要性 | 累计重要性阈值 | 大规模因子 |
| | L1正则化 | LogisticRegression(penalty='l1') | 自动稀疏化 |
| **包装法** | RFE递归消除 | `RFE(estimator, n_features)` | 精确但慢 |

### 5.2 特征选择流程建议

```
所有因子 → 过滤法(快速去噪声) → 嵌入法(RF重要性) → 最终特征集
```

**经验法则**:
- 因子数 < 30: 可以全部使用
- 因子数 30-100: 用 RF 重要性筛选 Top 20-30
- 因子数 > 100: 先过滤法 → 再嵌入法

### 5.3 衍生因子构造

| 原始因子 | 衍生方法 | 示例 |
|----------|----------|------|
| 价格 | 均线比值 | `MA5/MA20, MA20/MA60` |
| 成交量 | 量比 | `VOL5/VOL20` |
| 因子 | 一阶差分 | `factor(t) - factor(t-1)` |
| 因子 | 滚动统计 | `factor.rolling(20).mean()` |
| 多因子 | 交互项 | `ROE * BP` (质量价值) |

---

## 6. 模型选择指南

### 6.1 八大经典分类模型

| 模型 | 适用场景 | 超参数 | 优点 | 缺点 |
|------|----------|--------|------|------|
| **KNN** | 小样本, 局部结构 | n_neighbors | 简单, 非线性 | 计算慢, 维度灾难 |
| **逻辑回归** | 线性关系强 | C, penalty | 可解释, 稳定 | 只能线性 |
| **决策树** | 特征筛选 | max_depth | 可解释 | 易过拟合 |
| **朴素贝叶斯** | 基线模型 | - | 快 | 独立性假设太强 |
| **随机森林** | **最常用** | n_estimators, max_depth | 稳健, 抗过拟合 | 预测慢 |
| **AdaBoost** | 弱分类器组合 | n_estimators, learning_rate | 精度高 | 对噪声敏感 |
| **SVM** | 小样本高维 | C, kernel, gamma | 高维效果好 | 调参难, 慢 |
| **XGBoost** | **效果最好** | max_depth, learning_rate, n_estimators | 精度高, 快 | 易过拟合 |

### 6.2 模型选择决策树

```
因子与收益关系?
├── 线性为主 → 逻辑回归 / 线性SVM
├── 非线性为主 → 
│   ├── 因子数 < 50 → XGBoost / LightGBM (推荐)
│   ├── 因子数 50-200 → 随机森林
│   └── 因子数 > 200 → LightGBM + 特征选择
├── 需要预测区间 → 保形回归 (Conformal Prediction)
├── 时序依赖强 → RNN / 多任务学习
└── 端到端交易 → DQN 强化学习
```

### 6.3 无监督学习

| 方法 | 用途 | 参数 |
|------|------|------|
| **K-Means** | 股票/基金分类 | n_clusters=5~10 |
| **t-SNE** | 降维可视化 | perplexity=30 |
| **PCA** | 降维去相关 | n_components=0.95 |

### 6.4 深度学习

| 架构 | 用途 | 关键设计 |
|------|------|----------|
| **DNN** | 非线性回归/分类 | 3-5层, Dropout, BatchNorm |
| **RNN/LSTM** | 时序因子 | 捕捉时间依赖 |
| **多任务学习(MTL)** | 组合优化 | 直接输出权重, Sharpe Loss |
| **Autoencoder** | 状态表示 | DQN的状态压缩 |

---

## 7. 训练策略

### 7.1 时间序列划分

```python
# 禁止 random_split! 必须按时间划分
trade_days = get_monthly_dates('2010-01-01', '2020-12-31')

train_days = trade_days[:88]   # 前88个月训练
test_days  = trade_days[88:]   # 后36个月测试
```

### 7.2 滚动训练 (防止模型过时)

```python
def rolling_train(dates, window=60):
    """滚动窗口训练"""
    models = {}
    for i, date in enumerate(dates):
        if i < window:
            continue
        train_dates = dates[i-window:i]
        X_train, y_train = build_dataset(train_dates)
        
        model = XGBClassifier()
        model.fit(X_train, y_train)
        models[date] = model
    return models
```

| 策略 | 窗口大小 | 重训频率 | 适用场景 |
|------|----------|----------|----------|
| **固定窗口** | 60个月 | 每月 | 市场稳定 |
| **扩展窗口** | 从起点到当前 | 每月 | 样本少时 |
| **自适应** | IC衰减触发 | 动态 | 风格切换快 |

### 7.3 交叉验证

```python
# 时间序列 CV (禁止 KFold!)
from sklearn.model_selection import TimeSeriesSplit
tscv = TimeSeriesSplit(n_splits=5)

# 或 GridSearchCV (用普通CV, 但数据已按时间排序)
GridSearchCV(model, param_grid, cv=5, scoring='roc_auc')
```

### 7.4 超参数调优

**XGBoost 推荐搜索空间**:
```python
param_grid = {
    'max_depth': [3, 5, 7],
    'learning_rate': [0.01, 0.05, 0.1],
    'n_estimators': [50, 100, 200],
    'min_child_weight': [1, 3, 5],
    'subsample': [0.8, 1.0],
    'colsample_bytree': [0.8, 1.0],
}
```

---

## 8. 预测与组合构建

### 8.1 预测输出

```python
# 分类模型: 输出赢家概率
prob = model.predict_proba(X)[:, 1]  # P(y=1)

# 回归模型: 输出预期收益
expected_ret = model.predict(X)

# SVM: 用 decision_function
score = model.decision_function(X)
```

### 8.2 选股策略

| 策略 | 方法 | 代码 |
|------|------|------|
| **Top-N** | 选概率最高的N只 | `sort_values().head(N)` |
| **阈值法** | 概率 > threshold | `prob[prob > 0.6]` |
| **分层法** | 按分位数分组 | `pd.qcut(prob, 5)` |

### 8.3 权重分配

| 方法 | 公式 | 特点 |
|------|------|------|
| **等权** | `w = 1/N` | 简单, 常用 |
| **概率加权** | `w = prob / sum(prob)` | 置信度高权重高 |
| **风险平价** | `w ∝ 1/vol` | 控制风险 |
| **均值方差** | `w = Σ⁻μ / (1'Σ⁻μ)` | 理论最优, 不稳定 |

---

## 9. 回测评估体系

### 9.1 分层回测 (核心验证方法)

```python
def group_backtest(df, n_groups=5):
    """按预测分数分5组, 看收益是否单调"""
    df = df.sort_values('predict', ascending=False)
    size = len(df) // n_groups
    groups = {}
    for i in range(n_groups):
        group_stocks = df.iloc[i*size:(i+1)*size]
        groups[f'group{i+1}'] = group_stocks['return'].mean()
    return groups
```

**判断标准**:
- 好的模型: Group1 > Group2 > Group3 > Group4 > Group5 (单调递减)
- 如果 Group1 < Group5: 模型反向或过拟合

### 9.2 评估指标

| 指标 | 公式 | 含义 | 好标准 |
|------|------|------|--------|
| **累计收益率** | `∏(1+r)` | 总收益 | > 基准 |
| **年化收益率** | `(累计)^(12/n) - 1` | 年化水平 | > 10% |
| **最大回撤** | `max(峰值-谷值)/峰值` | 最大亏损 | < 20% |
| **夏普比率** | `(年化-无风险)/波动率` | 风险调整后收益 | > 1.0 |
| **Alpha** | 超额收益年化 | 跑赢基准能力 | > 5% |
| **信息比率** | `Alpha / 跟踪误差` | 超额收益稳定性 | > 0.5 |
| **胜率** | `正收益月数/总月数` | 稳定性 | > 55% |
| **Accuracy** | 预测正确率 | 分类精度 | > 52% |
| **AUC** | ROC曲线下面积 | 排序能力 | > 0.55 |

### 9.3 评价函数模板

```python
def evaluate(group_df, benchmark_ret, risk_free=0.04):
    """计算策略各项指标"""
    n = len(group_df)
    results = {}
    for col in group_df.columns:
        ret = group_df[col]
        cum_ret = (ret + 1).cumprod()[-1]
        ann_ret = cum_ret ** (12/n) - 1
        max_dd = ((cum_ret.cummax() - cum_ret) / cum_ret.cummax()).max()
        sharpe = (ann_ret - risk_free) / ret.std()
        
        excess = ret - benchmark_ret
        alpha = (excess + 1).cumprod()[-1] ** (12/n) - 1
        IR = alpha / excess.std()
        win_rate = (ret > 0).sum() / n
        beat_rate = (ret > benchmark_ret).sum() / n
        
        results[col] = {
            '累计收益': cum_ret - 1,
            '年化收益': ann_ret,
            '最大回撤': max_dd,
            '夏普比率': sharpe,
            'Alpha': alpha,
            '信息比率': IR,
            '胜率': win_rate,
            '跑赢基准': beat_rate,
        }
    return pd.DataFrame(results)
```

---

## 10. 通用代码模板

### 10.1 完整流水线

```python
import pandas as pd
import numpy as np
from jqdata import *
from jqfactor import *
from jqlib.technical_analysis import *
from sklearn.ensemble import RandomForestClassifier, GradientBoostingClassifier
from xgboost import XGBClassifier
import lightgbm as lgb
from sklearn.model_selection import GridSearchCV
import pickle

# ==================== 配置区 ====================
CONFIG = {
    'index': '399905.XSHE',          # 股票池
    'start_date': '2015-01-01',      # 回测起点
    'end_date': '2023-12-31',        # 回测终点
    'train_months': 60,              # 训练窗口
    'rebalance': 'monthly',          # 调仓频率
    'top_pct': 0.3,                  # 标签: 前30%
    'bottom_pct': 0.3,               # 标签: 后30%
    'n_stocks': 5,                   # 每次选股数
    'model': 'xgboost',              # 模型选择
    'transaction_cost': 0.003,       # 交易成本
}

# ==================== 1. 股票池筛选 ====================
def filter_stocks(date, index=CONFIG['index'], list_days=90):
    stocks = get_index_stocks(index, date)
    # 剔除ST
    st = get_extras('is_st', stocks, count=1, end_date=date)
    stocks = [s for s in stocks if not st[s][0]]
    # 剔除停牌
    paused = get_price(stocks, count=1, end_date=date, fields='paused', panel=False)
    stocks = list(paused[paused['paused'] != 1]['code'])
    # 剔除次新
    stocks = [s for s in stocks if get_security_info(s).start_date < date - pd.Timedelta(days=list_days)]
    return stocks

# ==================== 2. 因子计算 ====================
def get_factors(stocks, date):
    df = pd.DataFrame(index=stocks)
    
    # --- 财务估值因子 ---
    q = query(valuation.code, valuation.pe_ratio, valuation.pb_ratio,
              valuation.ps_ratio, valuation.pcf_ratio, valuation.market_cap)
    fund = get_fundamentals(q.filter(valuation.code.in_(stocks)), date=date)
    fund.set_index('code', inplace=True)
    df['EP'] = 1 / fund['pe_ratio']
    df['BP'] = 1 / fund['pb_ratio']
    df['SP'] = 1 / fund['ps_ratio']
    df['CFP'] = 1 / fund['pcf_ratio']
    df['market_cap'] = fund['market_cap']
    
    # --- 聚宽因子库 ---
    jq_factors = ['roe_ttm', 'roa_ttm', 'gross_income_ratio', 
                  'net_profit_to_total_operate_revenue_ttm',
                  'operating_revenue_growth_rate', 'net_profit_growth_rate']
    jq_data = get_factor_values(stocks, jq_factors, count=1, end_date=date)
    for f in jq_factors:
        df[f] = jq_data[f].iloc[0]
    
    # --- 技术因子 ---
    df['RSI'] = pd.Series(RSI(stocks, date, N1=20))
    dif, dea, macd = MACD(stocks, date, SHORT=10, LONG=30, MID=15)
    df['DIF'], df['DEA'], df['MACD'] = pd.Series(dif), pd.Series(dea), pd.Series(macd)
    df['BIAS'] = pd.Series(BIAS(stocks, date, N1=20)[0])
    
    return df

# ==================== 3. 预处理 ====================
def preprocess(df, date):
    # 去极值
    median = df.median()
    mad = (df - median).abs().median()
    df = df.clip(median - 5*mad, median + 5*mad, axis=1)
    
    # 行业均值填充
    industries = get_industry(df.index, date)
    for col in df.columns:
        df[col] = df.groupby(industries)[col].transform(lambda x: x.fillna(x.mean()))
    df = df.fillna(df.mean())
    
    # 中性化 (行业+市值)
    industry_dummies = pd.get_dummies(pd.Series(industries, index=df.index))
    ln_mv = np.log(df['market_cap'])
    for col in df.columns:
        X = sm.add_constant(pd.concat([industry_dummies, ln_mv], axis=1))
        y = df[col]
        valid = y.notnull() & X.notnull().all(axis=1)
        if valid.sum() > 10:
            res = sm.OLS(y[valid], X[valid]).fit()
            df[col] = res.resid
    
    # 标准化
    df = (df - df.mean()) / df.std()
    return df

# ==================== 4. 标签构建 ====================
def build_labels(stocks, date, next_date):
    ret = get_price(stocks, start_date=date, end_date=next_date, 
                    fields='close', panel=False)
    ret = ret.groupby('code')['close'].apply(lambda x: x.iloc[-1]/x.iloc[0] - 1)
    
    ret = ret.sort_values(ascending=False)
    n = len(ret)
    n_top = int(n * CONFIG['top_pct'])
    n_bottom = int(n * CONFIG['bottom_pct'])
    
    labels = pd.Series(np.nan, index=ret.index)
    labels.iloc[:n_top] = 1
    labels.iloc[-n_bottom:] = 0
    return labels.dropna()

# ==================== 5. 模型训练 ====================
def get_model(name='xgboost'):
    models = {
        'xgboost': XGBClassifier(max_depth=5, learning_rate=0.05, n_estimators=100),
        'random_forest': RandomForestClassifier(n_estimators=100, max_depth=5),
        'lightgbm': lgb.LGBMClassifier(n_estimators=100, max_depth=5),
        'logistic': LogisticRegression(C=1, penalty='l2', max_iter=300),
        'svm': SVC(kernel='rbf', C=1, gamma='scale', probability=True),
    }
    return models[name]

# ==================== 6. 主循环 ====================
def run_pipeline():
    dates = get_monthly_dates(CONFIG['start_date'], CONFIG['end_date'])
    portfolio_history = {}
    model = None
    
    for i, date in enumerate(dates[:-1]):
        next_date = dates[i + 1]
        
        # 获取股票池
        stocks = filter_stocks(date)
        if len(stocks) < 50:
            continue
        
        # 获取因子
        factors = get_factors(stocks, date)
        factors = preprocess(factors, date)
        
        # 构建训练集 (滚动窗口)
        if i >= CONFIG['train_months']:
            train_dates = dates[max(0, i-CONFIG['train_months']):i]
            X_train, y_train = [], []
            for td in train_dates:
                td_next = dates[train_dates.tolist().index(td) + 1]
                td_stocks = filter_stocks(td)
                if len(td_stocks) < 50:
                    continue
                td_factors = preprocess(get_factors(td_stocks, td), td)
                td_labels = build_labels(td_stocks, td, td_next)
                common = td_factors.index.intersection(td_labels.index)
                X_train.append(td_factors.loc[common])
                y_train.append(td_labels.loc[common])
            
            if X_train:
                X_train = pd.concat(X_train)
                y_train = pd.concat(y_train)
                model = get_model(CONFIG['model'])
                model.fit(X_train, y_train)
        
        # 预测选股
        if model is not None:
            prob = model.predict_proba(factors)[:, 1]
            top_stocks = pd.Series(prob, index=factors.index).nlargest(CONFIG['n_stocks']).index.tolist()
            portfolio_history[date] = {
                'stocks': top_stocks,
                'weights': {s: 1/CONFIG['n_stocks'] for s in top_stocks}
            }
    
    return portfolio_history
```

---

## 11. 关键注意事项

### 11.1 常见陷阱

| 陷阱 | 表现 | 解决方案 |
|------|------|----------|
| **未来函数** | 回测极好, 实盘极差 | 因子日期 < 交易日期 |
| **随机Split** | 训练集包含未来信息 | 严格按时间划分 |
| **过拟合** | 训练AUC=0.7, 测试AUC=0.5 | 简化模型, 增加正则 |
| **幸存者偏差** | 只用当前成分股 | 用历史成分股 |
| **换手过高** | 收益被手续费吃掉 | 增加换手惩罚 |
| **流动性忽略** | 小盘股买不到 | 过滤成交额 < 1000万 |

### 11.2 因子IC检验

```python
def factor_ic(factors, returns):
    """计算因子IC (信息系数)"""
    ics = []
    for col in factors.columns:
        ic = factors[col].corr(returns, method='spearman')
        ics.append(ic)
    return pd.Series(ics, index=factors.columns)

# IC > 0.03: 因子有效
# ICIR = mean(IC) / std(IC) > 0.5: 因子稳定
```

### 11.3 交易成本

| 成本类型 | 费率 | 说明 |
|----------|------|------|
| 佣金 | 0.03% (双边) | 券商收取 |
| 印花税 | 0.1% (卖出) | 仅卖出收取 |
| 滑点 | 0.1%-0.2% | 冲击成本 |
| **合计单边** | **~0.15%** | |
| **合计双边** | **~0.3%** | 一次调仓成本 |

---

## 12. 进阶方法

### 12.1 保形预测 (Conformal Prediction)

```python
# 5折保形回归, 给出预测区间
from conformal_prediction import ConformalRegressor
cr = ConformalRegressor(XGBRegressor(), n_folds=5)
prediction_interval = cr.predict(X_test, alpha=0.1)  # 90%置信区间
```

### 12.2 强化学习 DQN

```python
# 状态: 因子 + 持仓 + 市场状态
# 动作: 买入/卖出/持有
# 奖励: 组合收益 - 交易成本
# 网络: Feature Extractor → FC → Q(s,a)
```

### 12.3 多任务学习 (MTL)

```python
# 直接输出组合权重, 损失函数 = -Sharpe Ratio
# 共享底层特征, 多个任务头 (收益预测/风险预测)
```

### 12.4 模型融合

| 方法 | 实现 | 效果 |
|------|------|------|
| **投票法** | 多个模型投票 | 稳定 |
| **Stacking** | 元模型学习基模型输出 | 提升明显 |
| **Blending** | 加权平均预测 | 简单有效 |
| **动态权重** | 按近期表现调整权重 | 适应市场 |

---

## 附录 A: 因子词典

| 因子名 | 计算方式 | 预期方向 |
|--------|----------|----------|
| EP | 1/PE | 越大越好 (价值) |
| BP | 1/PB | 越大越好 (价值) |
| ROE | 净利润/净资产 | 越大越好 (质量) |
| 营收增长率 | (本期-上期)/上期 | 越大越好 (成长) |
| RSI | 相对强弱指标 | <30超买, >70超卖 |
| MACD-DIF | 快线-慢线 | 金叉买入 |
| 换手率 | 成交量/流通股本 | 低换手更佳 |
| 波动率 | 收益率标准差 | 低波动更佳 |

## 附录 B: 参考笔记本

| 文件 | 内容 |
|------|------|
| `78 八大机器学习模型大比拼` | 8种分类模型对比 |
| `12 机器学习用于量化分析` | XGBoost/RF/深度学习 |
| `92 动态多因子选股` | 决策树+SVR特征筛选 |
| `41 强化学习DQN` | DQN交易智能体 |
| `61 聚类算法分析股票` | K-Means股票分类 |
| `49 5折保形回归` | Conformal Prediction |
| `47 随机森林研究` | RF特征重要性 |
| `100 K-means基金分类` | 基金聚类 |
| `91 深度多任务学习` | MTL组合优化 |

---

*文档版本: v1.0 | 更新日期: 2026-04-20*
