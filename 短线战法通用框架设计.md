# 短线战法策略通用框架设计

## 一、策略类型概述

### 核心逻辑
A股短线战法的核心逻辑建立在三大制度性特征之上：
1. **涨停板制度**：10%/20%涨跌幅限制导致价格发现不充分，涨停股次日存在惯性溢价
2. **情绪周期**：市场情绪呈现"冰点→回暖→高潮→退潮"的周期性波动，连板高度是情绪温度计
3. **龙头溢价**：资金聚焦效应导致板块龙头获得超额流动性溢价

### 适用场景
- 市场情绪回暖期/上升期（跌停数<15，连板高度>3）
- 有明确主线题材的市场环境
- 适合分钟级别回测和交易

### 风险特征
- 高收益高回撤（年化50%-300%，回撤15%-40%）
- 胜率分化大（60%-90%不等）
- 对交易纪律要求极高，严格执行止盈止损

---

## 二、子策略变体分类

通过对约50个短线战法策略文件的分析，归纳出以下变体：

| 序号 | 策略名称 | 核心逻辑 | 代表文件 |
|------|----------|----------|----------|
| 1 | **首板低开** | 昨日首板+今日低开3-4%+相对低位 | `01 首板低开策略.txt`, `78 首板低开策略-终极版.txt` |
| 2 | **首板高开** | 昨日首板+今日高开1-6%+量价配合 | `13 首板高开-低开-弱转强混合策略.txt` |
| 3 | **龙回头** | 连板龙头回调20%+企稳信号+再次冲高 | `01 龙回头3.0回测速度优化版.txt`, `99最新龙回头5.0.txt` |
| 4 | **连板龙头** | 市场最高连板股+因子筛选 | `02 连板龙头策略.txt`, `07 连板龙头策略.txt` |
| 5 | **冲天炮** | 最高板+早盘冲天炮买入 | `20 冲天炮最高板策略.txt`, `95 冲天炮最高板策略迭代.txt` |
| 6 | **分歧反包** | 涨停后分歧收阴+次日反包涨停 | `06 87.5%胜率之分歧反包二板.txt`, `14 缩量分歧反包战法.txt` |
| 7 | **弱转强** | 昨日烂板/阴线+今日高开竞价走强 | `21 涨停弱转强竞价战法.txt`, `77 龙头弱转强战法.txt` |
| 8 | **集合竞价** | 竞价量比+高开幅度筛选 | `16 集合竞价三合一.txt`, `96 集合竞价量比策略V1.txt` |
| 9 | **龙头首阴** | 龙头首次收阴+次日反包预期 | `10 龙头首阴战法改版二.txt` |
| 10 | **龙头底分型** | 龙头回调底分型+涨停确认 | `02 龙头底分型战法-两年23倍.txt` |
| 11 | **单阳不破** | 大阳线后横盘不破+再次突破 | `52 龙头战法之单阳不破.txt` |
| 12 | **二板排板** | 首板后二板排涨停买入 | `10 10倍-二板排板战法优化.txt` |
| 13 | **炸板反包** | 昨日炸板+今日反包涨停 | `50 昨日炸板股策略.txt` |
| 14 | **打首板** | 盘中打首板涨停 | `88 打首板策略.txt` |
| 15 | **布林线追龙头** | 布林通道+均线多头追龙头 | `94 布林线追龙头战法.txt` |

---

## 三、通用代码框架

### 3.1 核心模块定义

#### 模块1: 涨停股识别器 (LimitUpDetector)

```python
class LimitUpDetector:
    """涨停股识别与连板分析模块"""
    
    def get_limit_up_stocks(self, stock_list: List[str], date: str) -> List[str]:
        """获取当日涨停股票（收盘价=涨停价）"""
        # 核心代码模式 (来自 01 首板低开策略.txt:167-172):
        # df = get_price(initial_list, end_date=date, frequency='daily', 
        #                fields=['close','high_limit'], count=1, panel=False)
        # df = df[df['close'] == df['high_limit']]
        # return list(df.code)
        pass
    
    def get_ever_limit_up_stocks(self, stock_list: List[str], date: str) -> List[str]:
        """获取当日曾涨停股票（最高价>=涨停价）"""
        # 核心代码模式 (来自 16 集合竞价三合一.txt:340-345):
        # df = get_price(initial_list, end_date=date, frequency='daily', 
        #                fields=['high','high_limit'], count=1, panel=False)
        # df = df[df['high'] == df['high_limit']]
        # return list(df.code)
        pass
    
    def get_ever_limit_up_not_closed(self, stock_list: List[str], date: str) -> List[str]:
        """获取当日曾涨停但未封板股票（炸板股）"""
        # 核心代码模式 (来自 16 集合竞价三合一.txt:348-355):
        # df = get_price(initial_list, end_date=date, frequency='daily', 
        #                fields=['close','high','high_limit'], count=1, panel=False)
        # df = df[(df['high'] == df['high_limit']) & (df['close'] != df['high_limit'])]
        # return list(df.code)
        pass
    
    def get_continuous_limit_up(self, stock_list: List[str], date: str, lookback: int = 10) -> Dict[str, int]:
        """获取连板股票及连板数"""
        # 核心代码模式 (来自 02 连板龙头策略.txt:279-296):
        # 遍历2到watch_days天，找出连续涨停天数
        # df = get_price(hl_list, end_date=date, frequency='daily', 
        #                fields=['close','high_limit','low'], count=watch_days, panel=False)
        # 逐日判断连续涨停，返回{stock: count}
        pass
    
    def get_first_limit_up(self, stock_list: List[str], date: str, lookback: int = 10) -> List[str]:
        """获取首板股票（近N日首次涨停，非连板）"""
        # 核心代码模式 (来自 88 打首板策略.txt:194-204):
        # df_panel_20 = get_price(st, count=60, end_date=end_date, frequency='daily', 
        #                         fields=['open','close','high_limit','money','high','low'])
        # sum_plus_num_20 = (df_panel_20.tail(8)['high'] == df_panel_20.tail(8)['high_limit']).sum()
        # if sum_plus_num_20 == 1:  # 近8日首次触及涨停
        pass
    
    def get_max_continuous_limit_up(self, date: str, lookback: int = 20) -> Tuple[int, List[str]]:
        """获取市场最高连板数及对应股票"""
        # 核心代码模式 (来自 02 连板龙头策略.txt:48-57):
        # ccd = get_continue_count_df(hl_list, date, 20)
        # M = ccd['count'].max()  # 最高连板数
        # CCD = ccd[ccd['count'] == M]  # 龙头股
        # lt = list(CCD.index)
        pass
```

#### 模块2: 形态识别器 (PatternRecognizer)

```python
class PatternRecognizer:
    """K线形态与技术位识别模块"""
    
    def is_low_open(self, stock: str, date: str, threshold: Tuple[float, float] = (0.96, 0.97)) -> bool:
        """是否低开（开盘价/昨收在阈值范围内）"""
        # 核心代码模式 (来自 01 首板低开策略.txt:51-54):
        # df['open_pct'] = current_data[s].day_open / df.loc[s, 'close']
        # df = df[(0.96 <= df['open_pct']) & (df['open_pct'] <= 0.97)]
        pass
    
    def is_high_open(self, stock: str, date: str, threshold: Tuple[float, float] = (1.01, 1.06)) -> bool:
        """是否高开（开盘价/昨收在阈值范围内）"""
        # 核心代码模式 (来自 21 涨停弱转强竞价战法.txt:87-89):
        # current_ratio = auction_data['current'][0] / (current_data[s].high_limit/1.1)
        # if current_ratio <= 1 or current_ratio >= 1.06:  # 高开1-6%
        pass
    
    def is_relative_low(self, stock: str, date: str, lookback: int = 60, threshold: float = 0.5) -> bool:
        """是否相对低位（收盘价在过去N日高低点区间的相对位置）"""
        # 核心代码模式 (来自 01 首板低开策略.txt:212-221):
        # df = get_price(stock_list, end_date=date, fields=['high','low','close'], count=watch_days, panel=False)
        # rp = (close - low) / (high - low)  # 相对位置
        # if rp <= 0.5:  # 处于下半区
        pass
    
    def is_pullback(self, stock: str, date: str, lookback: int = 12, threshold: float = 0.20) -> bool:
        """是否回调（从高点回撤超过阈值，龙回头核心条件）"""
        # 核心代码模式 (来自 01 龙回头3.0.txt:240-249):
        # max_price = max(historys['high'][lianxuid:])  # 连板后最高点
        # min_price = min(historys['low'][lianxuid:])   # 连板后最低点
        # if (max_price - min_price) / min_price < 0.2:  # 回调幅度<20%不满足
        pass
    
    def is_fanbao(self, stock: str, date: str) -> bool:
        """是否反包（今日收盘价>昨日最高价，或今日涨停）"""
        # 核心代码模式 (来自 33 大阳分歧反包.txt:141):
        # if df_close < df_high * 0.976 and df_close > df_pre_close:  # 昨日分歧
        # 次日: if current_price > pre_high and current_price > pre_close * 1.06:  # 反包
        pass
    
    def is_weak_to_strong(self, stock: str, date: str) -> bool:
        """是否弱转强（昨日烂板/阴线+今日高开走强）"""
        # 核心代码模式 (来自 77 龙头弱转强战法.txt:128-136):
        # if hisprice['close'][-1] / hisprice['close'][-2] < 0.911:  # 昨日未涨停或下跌
        # if hisprice['close'][-1] / hisprice['close'][-2] > 1.099:  # 昨日未涨停
        # 次日: if current_data[security].day_open > hisprice[security][0]:  # 今日高开
        pass
    
    def is_bottom_fenxing(self, stock: str, date: str, lookback: int = 3) -> bool:
        """是否底分型（中间K线低点最低，两边较高）"""
        # 核心代码模式 (来自 02 龙头底分型战法.txt:267):
        # if (df_close_three == df_high_limit_three and  # 第三日涨停
        #     df_open_one > df_close_two * 1.02 and      # 第一日开盘>第二日收盘
        #     df_close_three > df_close_two * 1.07):     # 第三日收盘>第二日收盘
        pass
    
    def is_yingxian(self, open: float, close: float, high: float, low: float, preclose: float) -> bool:
        """是否实体阴线（跌幅>3%，收阴）"""
        # 核心代码模式 (来自 01 龙回头3.0.txt:306-313):
        # if close > open or close > preclose: return False
        # if (preclose - close) / preclose < 0.03: return False  # 跌幅<3%不算
        # return True
        pass
    
    def calculate_zyts(self, stock: str, context, lookback: int = 101) -> int:
        """计算左压天数（上次高点至今的天数）"""
        # 核心代码模式 (来自 16 集合竞价三合一.txt:323-328):
        # high_prices = attribute_history(s, 101, '1d', fields=['high'], skip_paused=True)['high']
        # prev_high = high_prices.iloc[-1]
        # zyts_0 = next((i-1 for i, high in enumerate(high_prices[-3::-1], 2) if high >= prev_high), 100)
        # return zyts_0 + 5
        pass
```

#### 模块3: 情绪指标计算器 (SentimentCalculator)

```python
class SentimentCalculator:
    """市场情绪监控模块"""
    
    def get_limit_up_count(self, date: str) -> int:
        """当日涨停数量"""
        pass
    
    def get_limit_down_count(self, date: str) -> int:
        """当日跌停数量（风控核心指标）"""
        # 核心代码模式 (来自 99最新龙回头5.0.txt:382-398):
        # pre_data = get_price(security_list, end_date=context.previous_date, 
        #                      frequency='daily', fields=['close','low_limit'], count=1, panel=False)
        # pre_limit_down = pre_data[pre_data.close == pre_data.low_limit]
        # pre_limit_down_num = len(pre_limit_down)
        # if pre_limit_down_num > 18:  # 跌停超过18只，风险大
        pass
    
    def get_continuous_limit_up_distribution(self, date: str) -> Dict[int, int]:
        """连板数分布（2板、3板、4板...各有多少只）"""
        pass
    
    def get_max_continuous_limit_up(self, date: str) -> int:
        """市场最高连板数（情绪温度核心指标）"""
        # 核心代码模式 (来自 20 冲天炮最高板策略.txt:153-154):
        # g.max_zt_days = df[df.day_count == review_days].ups.max()
        pass
    
    def get_limit_up_promotion_rate(self, date: str) -> float:
        """涨停晋级率（昨日涨停今日继续涨停的比例）"""
        # 核心代码模式 (来自 02 连板龙头策略.txt:347):
        # r = 100 * len(CCD) / len(hl_list) if len(hl_list) != 0 else 0
        pass
    
    def get_market_sentiment_score(self, date: str) -> float:
        """市场情绪综合得分（0-100）"""
        # 综合: 最高连板数、涨停数量、跌停数量、晋级率
        # 核心代码模式 (来自 02 连板龙头策略.txt:348-353):
        # emo = M  # 最高连板数
        # cyc = 1 if g.emo_count[-1] == max(g.emo_count[-3:]) else 0  # 情绪周期
        pass
    
    def is_market_risk(self, context) -> bool:
        """判断市场是否存在系统性风险"""
        # 核心代码模式 (来自 99最新龙回头5.0.txt:361-398):
        # 1. 大盘跌幅: dapan_rise = (df.close[-1] - df.close[-2]) / df.close[-2]
        # 2. 跌停数量趋势: if pre_limit_down_num >= pre_pre_limit_down_num and pre_limit_down_num > 18
        # 3. 指数MACD: if df_idx['macdhist'][-1] < df_idx['macdhist'][-2]:  # MACD柱缩短
        pass
```

#### 模块4: 买入信号生成器 (BuySignalGenerator)

```python
class BuySignalGenerator:
    """买入信号生成模块（策略差异化的核心）"""
    
    def generate_first_limit_low_open(self, context) -> List[str]:
        """首板低开策略信号"""
        # 核心代码模式 (来自 01 首板低开策略.txt:29-61):
        # 1. 获取昨日涨停列表
        # 2. 排除连板股（只留首板）
        # 3. 相对位置 <= 0.5
        # 4. 低开 96%-97%
        # 5. 等权买入
        pass
    
    def generate_first_limit_high_open(self, context) -> List[str]:
        """首板高开策略信号"""
        # 核心代码模式 (来自 13 首板高开-低开-弱转强混合策略.txt:107-136):
        # 1. 均价涨幅 > 7%
        # 2. 成交额 5.5亿-20亿
        # 3. 总市值 > 70亿，流通市值 < 520亿
        # 4. 高开 1-6%
        # 5. 竞价量/昨日量 > 3%
        # 6. 左压以来放量
        pass
    
    def generate_long_huitou(self, context) -> List[str]:
        """龙回头策略信号"""
        # 核心代码模式 (来自 01 龙回头3.0.txt:191-296):
        # 1. 有连续涨停（2板以上）
        # 2. 连板后回调幅度 >= 20%
        # 3. 回调时间 >= 3天
        # 4. 回调中有冲高（日内涨幅>4.5%）
        # 5. 阳线数量 <= 1
        # 6. 最后出现阴线（实体跌幅>3%）
        # 7. 当日涨幅 > 5% 且 开盘价 < 当前价
        pass
    
    def generate_continuous_leader(self, context) -> List[str]:
        """连板龙头策略信号"""
        # 核心代码模式 (来自 02 连板龙头策略.txt:35-66):
        # 1. 获取市场最高连板数
        # 2. 筛选最高连板股票（龙头）
        # 3. 因子筛选（如VOL5换手率最低）
        # 4. 开盘涨停则限价排板，未涨停则市价买入
        pass
    
    def generate_fanbao(self, context) -> List[str]:
        """分歧反包策略信号"""
        # 核心代码模式 (来自 14 缩量分歧反包战法.txt:174-213):
        # 1. 筛选近期连板股（2板以上）
        # 2. 昨日未封板（close < high_limit * 0.98）
        # 3. 今日: current_price > pre_close * 1.05
        # 4. 今日: day_open > pre_close
        # 5. 今日: current_price > day_open
        pass
    
    def generate_weak_to_strong(self, context) -> List[str]:
        """弱转强策略信号"""
        # 核心代码模式 (来自 21 涨停弱转强竞价战法.txt:55-103):
        # 1. 昨日首板（非连板）
        # 2. 均价涨幅 > 7%
        # 3. 成交额 > 7亿
        # 4. 总市值 > 70亿
        # 5. 高开 1-6%
        # 6. 竞价量/昨日量 > 3%
        pass
    
    def generate_call_auction(self, context) -> List[str]:
        """集合竞价策略信号"""
        # 核心代码模式 (来自 16 集合竞价三合一.txt:84-227):
        # 三合一: 一进二高开 + 首板低开 + 弱转强
        # 竞价时间: 09:15-09:26
        # auction_data = get_call_auction(s, start_date=start, end_date=end, 
        #                                 fields=['time','volume','current'])
        pass
    
    def generate_chongtianpao(self, context) -> List[str]:
        """冲天炮策略信号"""
        # 核心代码模式 (来自 20 冲天炮最高板策略.txt:35-69):
        # 1. 市场最高板 > 2
        # 2. 大盘趋势向上（线性拟合斜率>0）
        # 3. 早盘11点前买入
        # 4. 排除已涨停股票
        pass
```

#### 模块5: 卖出信号生成器 (SellSignalGenerator)

```python
class SellSignalGenerator:
    """卖出信号生成模块（所有策略共性最强部分）"""
    
    def generate_intraday_take_profit(self, context, time: str = "11:28") -> List[str]:
        """日内止盈信号（上午有利润就跑）"""
        # 核心代码模式 (来自 01 首板低开策略.txt:72-77):
        # if str(context.current_dt)[-8:] == '11:28:00':
        #     for s in context.portfolio.positions:
        #         if (closeable_amount != 0 and 
        #             last_price < high_limit and  # 未涨停
        #             last_price > avg_cost):      # 有利润
        #             order_target_value(s, 0)
        pass
    
    def generate_intraday_stop_loss(self, context, time: str = "14:50") -> List[str]:
        """日内止损信号（尾盘清仓）"""
        # 核心代码模式 (来自 01 首板低开策略.txt:79-84):
        # if str(context.current_dt)[-8:] == '14:50:00':
        #     for s in context.portfolio.positions:
        #         if (closeable_amount != 0 and 
        #             last_price < high_limit):    # 未涨停就卖
        #             order_target_value(s, 0)
        pass
    
    def generate_limit_open_sell(self, context) -> List[str]:
        """涨停打开卖出"""
        # 核心代码模式 (来自 02 连板龙头策略.txt:96-115):
        # if not (last_price == high_limit):  # 不涨停
        #     if closeable_amount != 0:
        #         if current_date >= target_date or ret > 0:  # 持满2天或盈利
        #             order_target_value(s, 0)
        pass
    
    def generate_time_based_sell(self, context, time: str = "13:42") -> List[str]:
        """时间止损卖出（龙回头专用）"""
        # 核心代码模式 (来自 01 龙回头3.0.txt:101-119):
        # if hour == 13 and minute == 42:
        #     if (closeprice - precloseprice) / precloseprice >= 0.096:
        #         continue  # 涨幅>9.6%不卖
        #     order_target(sec, 0)
        pass
    
    def generate_cost_based_stop_loss(self, context, threshold: float = -0.05) -> List[str]:
        """成本止损（亏损超过阈值）"""
        # 核心代码模式 (来自 95 冲天炮最高板策略迭代.txt:63-65):
        # if last_price < avg_cost * 0.95:  # 亏损>5%
        #     order_target(security, 0)
        pass
    
    def generate_ma_based_stop_loss(self, context, ma_period: int = 5) -> List[str]:
        """均线止损（跌破MA5）"""
        # 核心代码模式 (来自 13 首板高开-低开-弱转强混合策略.txt:71-74):
        # MA5 = close_data['close'].mean()
        # if last_price < MA5:
        #     order_target_value(s, 0)
        pass
    
    def generate_high_drawdown_sell(self, context, threshold: float = 0.93) -> List[str]:
        """日内高点回撤卖出"""
        # 核心代码模式 (来自 10 龙头首阴战法改版二.txt:142-146):
        # if current_price < df_max_high * 0.869:  # 从日内高点回撤>13%
        #     order_target(stock_two, 0)
        pass
    
    def generate_limit_down_sell(self, context) -> List[str]:
        """跌停风控卖出"""
        # 核心代码模式 (来自 78 首板低开策略-终极版.txt:80-112):
        # if close_price == low_limit and low_price == low_limit:  # 跌停
        #     order_target_value(stock, 0)
        # if loss_ratio <= -4:  # 亏损>4%
        #     order_target_value(stock, 0)
        pass
```

### 3.2 框架配置类

```python
from dataclasses import dataclass, field
from typing import Tuple, List, Dict, Optional
from enum import Enum

class StrategyType(Enum):
    FIRST_LIMIT_LOW_OPEN = "first_limit_low_open"      # 首板低开
    FIRST_LIMIT_HIGH_OPEN = "first_limit_high_open"    # 首板高开
    LONG_HUITOU = "long_huitou"                        # 龙回头
    CONTINUOUS_LEADER = "continuous_leader"            # 连板龙头
    CHONGTIANPAO = "chongtianpao"                      # 冲天炮
    FANBAO = "fanbao"                                  # 分歧反包
    WEAK_TO_STRONG = "weak_to_strong"                  # 弱转强
    CALL_AUCTION = "call_auction"                      # 集合竞价
    FIRST_YIN = "first_yin"                            # 龙头首阴
    BOTTOM_FENXING = "bottom_fenxing"                  # 龙头底分型
    DAN_YANG_BU_PO = "dan_yang_bu_po"                  # 单阳不破
    ERBAN_PAIBAN = "erban_paiban"                      # 二板排板
    ZHA_BAND_FANBAO = "zhaban_fanbao"                  # 炸板反包
    DA_SHOUBAN = "da_shouban"                          # 打首板

@dataclass
class ShortTermTradingConfig:
    """短线战法通用配置类"""
    
    # ==================== 策略类型 ====================
    strategy_type: StrategyType = StrategyType.FIRST_LIMIT_LOW_OPEN
    
    # ==================== 买入条件 ====================
    # 低开/高开范围 (相对于昨收)
    low_open_range: Tuple[float, float] = (0.96, 0.97)       # 首板低开: 低开3-4%
    high_open_range: Tuple[float, float] = (1.01, 1.06)      # 首板高开: 高开1-6%
    
    # 相对位置
    relative_position_threshold: float = 0.5                 # 处于N日下半区
    relative_position_lookback: int = 60                     # 相对位置计算周期
    
    # 龙回头回调参数
    pullback_threshold: float = 0.20                         # 回调幅度>=20%
    pullback_lookback: int = 12                              # 回调观察周期
    pullback_min_days: int = 3                               # 回调最少天数
    
    # 连板参数
    min_continuous_count: int = 2                            # 最少连板数
    max_continuous_count: int = 20                           # 最多连板数
    target_max_board: bool = True                            # 是否只买最高板
    
    # 竞价参数
    auction_volume_ratio: float = 0.03                       # 竞价量/昨日量 > 3%
    auction_time_start: str = "09:15:00"
    auction_time_end: str = "09:26:00"
    
    # 量价参数
    min_turnover_amount: float = 1e8                         # 最小成交额(1亿)
    max_turnover_amount: float = 20e8                        # 最大成交额(20亿)
    min_market_cap: float = 70                               # 最小总市值(亿)
    max_circulating_cap: float = 520                         # 最大流通市值(亿)
    avg_price_increase_threshold: float = 0.07               # 均价涨幅>7%
    
    # 左压参数
    volume_amplification_ratio: float = 0.9                  # 成交量放大比例
    
    # ==================== 卖出条件 ====================
    # 时间止盈止损
    intraday_take_profit_time: str = "11:28"                 # 日内止盈时间
    intraday_stop_loss_time: str = "14:50"                   # 日内止损时间
    long_huitou_sell_time: str = "13:42"                     # 龙回头卖出时间
    
    # 涨停持有
    hold_if_limit_up: bool = True                            # 涨停是否继续持有
    
    # 成本止损
    cost_stop_loss_threshold: float = -0.05                  # 亏损>5%止损
    
    # 均线止损
    ma_stop_loss_period: int = 5                             # MA均线止损周期
    
    # 高点回撤止损
    high_drawdown_threshold: float = 0.93                    # 从日内高点回撤>7%
    
    # 持仓时间
    min_hold_days: int = 0                                   # 最少持仓天数
    max_hold_days: int = 2                                   # 最多持仓天数
    
    # ==================== 风控配置 ====================
    # 市场风控
    max_limit_down_count: int = 18                           # 最大跌停数
    min_max_board: int = 3                                   # 最低连板高度
    enable_market_risk_check: bool = True                    # 是否启用市场风控
    
    # 股票池过滤
    exclude_kcb: bool = True                                 # 排除科创板(688)
    exclude_bse: bool = True                                 # 排除北交所(4/8)
    exclude_cyb: bool = False                                # 排除创业板(300/301)
    exclude_st: bool = True                                  # 排除ST
    exclude_delisting: bool = True                           # 排除退市股
    new_stock_days: int = 50                                 # 新股过滤天数
    exclude_extreme_limit_open: bool = True                  # 排除一字涨停
    
    # ==================== 仓位配置 ====================
    max_positions: int = 1                                   # 最大持仓数
    position_mode: str = "equal"                             # equal/fixed
    fixed_position_value: float = 0                          # 固定仓位金额
    cash_reserve_ratio: float = 0.0                          # 现金保留比例
    
    # ==================== 因子筛选 ====================
    factor_name: Optional[str] = None                        # 筛选因子(如VOL5)
    factor_sort_asc: bool = True                             # 因子排序方向
```

### 3.3 框架主类

```python
from abc import ABC, abstractmethod
from datetime import datetime
from typing import List, Dict, Optional

class BaseShortTermStrategy(ABC):
    """短线战法抽象基类"""
    
    def __init__(self, config: ShortTermTradingConfig):
        self.config = config
        self.limit_detector = LimitUpDetector()
        self.pattern_recognizer = PatternRecognizer()
        self.sentiment_calc = SentimentCalculator()
        self.buy_signal_gen = self._create_buy_generator()
        self.sell_signal_gen = SellSignalGenerator()
        self.target_list = []
        
    @abstractmethod
    def _create_buy_generator(self) -> BuySignalGenerator:
        """子类实现具体的买入信号生成器"""
        pass
    
    def initialize(self, context):
        """初始化"""
        set_option('use_real_price', True)
        set_option('avoid_future_data', True)
        log.set_level('system', 'error')
        
        # 定时任务
        run_daily(self.before_trading, '09:01')
        run_daily(self.buy, '09:30')
        run_daily(self.sell, self.config.intraday_take_profit_time)
        run_daily(self.sell, self.config.intraday_stop_loss_time)
    
    def before_trading(self, context):
        """盘前准备：筛选目标股票池"""
        date = transform_date(context.previous_date, 'str')
        initial_list = self._prepare_stock_pool(context, date)
        self.target_list = self.buy_signal_gen.generate(context, initial_list)
    
    def buy(self, context):
        """执行买入"""
        current_data = get_current_data()
        
        # 市场风控检查
        if self.config.enable_market_risk_check:
            if self.sentiment_calc.is_market_risk(context):
                return
        
        # 仓位检查
        available_slots = self.config.max_positions - len(context.portfolio.positions)
        if available_slots <= 0:
            return
        
        # 计算每只股票买入金额
        if self.config.position_mode == "equal":
            value = context.portfolio.available_cash / max(len(self.target_list), 1)
        else:
            value = self.config.fixed_position_value
        
        for s in self.target_list[:available_slots]:
            if context.portfolio.available_cash / current_data[s].last_price < 100:
                continue
            
            # 涨停排板 vs 市价买入
            if current_data[s].last_price == current_data[s].high_limit:
                order_value(s, value, LimitOrderStyle(current_data[s].day_open))
            else:
                order_value(s, value, MarketOrderStyle(current_data[s].day_open))
            
            print(f'买入 {s}')
    
    def sell(self, context):
        """执行卖出"""
        current_time = str(context.current_dt)[-8:]
        current_data = get_current_data()
        
        for s in list(context.portfolio.positions):
            if context.portfolio.positions[s].closeable_amount == 0:
                continue
            
            last_price = current_data[s].last_price
            high_limit = current_data[s].high_limit
            avg_cost = context.portfolio.positions[s].avg_cost
            
            # 涨停不卖
            if last_price == high_limit and self.config.hold_if_limit_up:
                continue
            
            # 根据时间执行不同卖出逻辑
            if current_time == f'{self.config.intraday_take_profit_time}:00':
                if last_price > avg_cost:  # 有利润
                    order_target_value(s, 0)
                    print(f'止盈卖出 {s}')
            
            elif current_time == f'{self.config.intraday_stop_loss_time}:00':
                order_target_value(s, 0)
                print(f'止损卖出 {s}')
    
    def _prepare_stock_pool(self, context, date: str) -> List[str]:
        """准备初始股票池"""
        initial_list = get_all_securities('stock', date).index.tolist()
        
        # 过滤
        if self.config.exclude_kcb:
            initial_list = [s for s in initial_list if not s.startswith('688')]
        if self.config.exclude_bse:
            initial_list = [s for s in initial_list if s[0] not in ('4', '8')]
        if self.config.exclude_cyb:
            initial_list = [s for s in initial_list if not s.startswith(('300', '301'))]
        if self.config.exclude_st:
            initial_list = self._filter_st(initial_list, date)
        if self.config.exclude_delisting:
            current_data = get_current_data()
            initial_list = [s for s in initial_list if '退' not in current_data[s].name]
        
        # 过滤新股
        initial_list = [s for s in initial_list 
                       if (context.current_dt.date() - get_security_info(s).start_date).days > self.config.new_stock_days]
        
        # 过滤停牌
        initial_list = self._filter_paused(initial_list, date)
        
        return initial_list
    
    def _filter_st(self, stock_list: List[str], date: str) -> List[str]:
        """过滤ST股票"""
        df = get_extras('is_st', stock_list, start_date=date, end_date=date, df=True)
        df = df.T
        df.columns = ['is_st']
        df = df[df['is_st'] == False]
        return list(df.index)
    
    def _filter_paused(self, stock_list: List[str], date: str) -> List[str]:
        """过滤停牌股票"""
        df = get_price(stock_list, end_date=date, frequency='daily', 
                      fields=['paused'], count=1, panel=False, fill_paused=True)
        df = df[df['paused'] == 0]
        return list(df.code)


# ==================== 具体策略实现示例 ====================

class FirstLimitLowOpenStrategy(BaseShortTermStrategy):
    """首板低开策略"""
    
    def _create_buy_generator(self) -> BuySignalGenerator:
        return FirstLimitLowOpenGenerator(self.config)


class LongHuitouStrategy(BaseShortTermStrategy):
    """龙回头策略"""
    
    def _create_buy_generator(self) -> BuySignalGenerator:
        return LongHuitouGenerator(self.config)


class ContinuousLeaderStrategy(BaseShortTermStrategy):
    """连板龙头策略"""
    
    def _create_buy_generator(self) -> BuySignalGenerator:
        return ContinuousLeaderGenerator(self.config)
```

---

## 四、典型策略映射示例

### 4.1 首板低开策略 (wywy1995)

**代表文件**: `01 首板低开策略.txt`, `78 首板低开策略-终极版.txt`

```python
config = ShortTermTradingConfig(
    strategy_type=StrategyType.FIRST_LIMIT_LOW_OPEN,
    low_open_range=(0.96, 0.97),           # 低开3-4%
    relative_position_threshold=0.5,       # 60日相对位置<=50%
    relative_position_lookback=60,
    max_positions=1,
    intraday_take_profit_time="11:28",     # 上午有利润就跑
    intraday_stop_loss_time="14:50",       # 尾盘清仓
    new_stock_days=250,
)
```

**核心逻辑**:
1. 获取昨日涨停股
2. 排除连板股（只留首板）
3. 筛选相对低位（60日下半区）
4. 今日低开3-4%
5. 买入后：11:28止盈（有利润且未涨停），14:50清仓（未涨停就卖）

### 4.2 龙回头3.0策略

**代表文件**: `01 龙回头3.0回测速度优化版.txt`, `99最新龙回头5.0速度优化版.txt`

```python
config = ShortTermTradingConfig(
    strategy_type=StrategyType.LONG_HUITOU,
    min_continuous_count=2,                # 至少2连板
    pullback_threshold=0.20,               # 回调>=20%
    pullback_lookback=12,
    pullback_min_days=3,                   # 回调至少3天
    max_positions=2,
    long_huitou_sell_time="13:42",         # 13:42统一卖出
    enable_market_risk_check=True,         # 大盘+跌停风控
    max_limit_down_count=18,               # 跌停>18只停止交易
)
```

**核心逻辑**:
1. 筛选有连板历史的股票（2板以上）
2. 连板后回调幅度>=20%
3. 回调时间>=3天
4. 回调中有冲高（日内涨幅>4.5%）
5. 阳线<=1根
6. 最后出现实体阴线（跌幅>3%）
7. 当日涨幅>5%且开盘<当前价时买入
8. 13:42统一卖出（涨幅>9.6%除外）

### 4.3 连板龙头策略

**代表文件**: `02 连板龙头策略.txt`, `07 连板龙头策略.txt`

```python
config = ShortTermTradingConfig(
    strategy_type=StrategyType.CONTINUOUS_LEADER,
    target_max_board=True,                 # 只买最高板
    min_continuous_count=2,
    max_positions=10,                      # 龙头一般不超过10个
    factor_name="VOL5",                    # 5日平均换手率
    factor_sort_asc=True,                  # 换手率最低
    min_hold_days=2,                       # 至少持有2天
    hold_if_limit_up=True,                 # 涨停继续持有
)
```

**核心逻辑**:
1. 获取市场最高连板数
2. 筛选最高连板股票（龙头）
3. 因子筛选（换手率最低等）
4. 开盘涨停则限价排板，未涨停则市价买入
5. 不涨停时：持有>=2天或盈利即卖出

### 4.4 分歧反包二板策略

**代表文件**: `06 87.5%胜率之分歧反包二板.txt`, `14 缩量分歧反包战法.txt`

```python
config = ShortTermTradingConfig(
    strategy_type=StrategyType.FANBAO,
    min_continuous_count=2,                # 2板以上
    max_positions=1,
    high_open_range=(1.05, None),          # 高开>5%
    intraday_stop_loss_time="14:40",
    cost_stop_loss_threshold=-0.09,        # 亏损>9%止损
)
```

**核心逻辑**:
1. 筛选近期连板股（2板以上）
2. 昨日分歧收阴（close < high_limit * 0.98）
3. 今日高开>5%
4. 当前价>开盘价*1.02
5. 当前价>昨日最高价（反包确认）
6. 多种条件止损：高点回撤、成本亏损、低开低走等

### 4.5 涨停弱转强竞价战法

**代表文件**: `21 【原创】年化123%最大回撤14%涨停弱转强竞价战法.txt`

```python
config = ShortTermTradingConfig(
    strategy_type=StrategyType.WEAK_TO_STRONG,
    high_open_range=(1.01, 1.06),          # 高开1-6%
    auction_volume_ratio=0.03,             # 竞价量>昨日3%
    min_turnover_amount=7e8,               # 成交额>7亿
    min_market_cap=70,                     # 总市值>70亿
    avg_price_increase_threshold=0.07,     # 均价涨幅>7%
    max_positions=3,
    multiple_sell_times=["09:30", "10:30", "13:30", "14:50"],
)
```

**核心逻辑**:
1. 昨日首板（非连板）
2. 均价涨幅>7%，成交额>7亿，市值>70亿
3. 高开1-6%，竞价量/昨日量>3%
4. 左压放量
5. 多时间点止损：9:30亏3%卖，10:30成本价卖，13:30亏3%卖，14:50清仓

### 4.6 冲天炮最高板策略

**代表文件**: `20 冲天炮最高板策略，收益惊呆了我.txt`, `95 冲天炮最高板策略迭代.txt`

```python
config = ShortTermTradingConfig(
    strategy_type=StrategyType.CHONGTIANPAO,
    target_max_board=True,
    min_max_board=3,                       # 最高板>=3
    max_positions=1,
    enable_market_risk_check=True,         # 线性拟合判断大盘趋势
    cost_stop_loss_threshold=-0.05,        # 亏损>5%止损
    intraday_stop_loss_time="14:55",       # 尾盘清仓
)
```

**核心逻辑**:
1. 计算市场最高连板数
2. 最高板>=3
3. 大盘趋势向上（线性拟合斜率>0）
4. 早盘11点前买入最高板股票
5. 亏损>5%止损，14:55未涨停清仓

---

## 五、策略扩展指南

### 5.1 新增策略变体

要新增一个短线战法变体，只需：

1. **继承 BuySignalGenerator**，实现 `generate()` 方法
2. **在 StrategyType 枚举中添加新类型**
3. **配置对应的参数**

```python
class NewStrategyGenerator(BuySignalGenerator):
    def generate(self, context, initial_list: List[str]) -> List[str]:
        # 1. 获取昨日涨停股
        hl_list = self.limit_detector.get_limit_up_stocks(initial_list, date)
        
        # 2. 根据策略逻辑筛选
        # ...
        
        # 3. 返回目标股票列表
        return target_list
```

### 5.2 常见扩展方向

| 扩展方向 | 说明 | 示例 |
|----------|------|------|
| **多策略融合** | 同时运行多个子策略 | `16 集合竞价三合一.txt` 融合了3个策略 |
| **情绪择时** | 根据市场情绪决定是否交易 | `99最新龙回头5.0.txt` 的跌停数风控 |
| **因子增强** | 在龙头筛选中加入因子 | `02 连板龙头策略.txt` 的VOL5因子 |
| **概念热点** | 结合热门概念筛选 | `02 连板龙头策略.txt` 注释中的概念筛选 |
| **动态仓位** | 根据情绪调整仓位 | 冲天炮策略的 `ratio` 动态调整 |

### 5.3 关键注意事项

1. **未来函数防范**：买入时间必须在09:25之后（知道开盘价），`set_option('avoid_future_data', True)`
2. **一字涨停过滤**：`low == high_limit` 的涨停无法买入，需排除
3. **涨停排板**：开盘涨停时使用 `LimitOrderStyle(day_open)` 排队
4. **T+1限制**：当日买入次日才能卖出
5. **分钟回测**：短线策略必须使用分钟级别回测才能准确反映交易逻辑
6. **滑点设置**：实盘需考虑滑点影响

### 5.4 通用卖出逻辑汇总

所有短线战法共性的卖出规则：

| 卖出类型 | 触发条件 | 适用策略 |
|----------|----------|----------|
| **涨停持有** | 涨停不卖 | 所有 |
| **日内止盈** | 11:28，有利润且未涨停 | 首板低开/高开 |
| **尾盘清仓** | 14:50，未涨停就卖 | 首板类/冲天炮 |
| **时间止损** | 13:42统一卖出 | 龙回头 |
| **成本止损** | 亏损>5% | 冲天炮/分歧反包 |
| **均线止损** | 跌破MA5 | 混合策略 |
| **高点回撤** | 从日内高点回撤>7% | 龙头首阴/打板 |
| **跌停止损** | 昨日跌停/今日跌停 | 所有（风控） |

---

## 六、完整文件索引

### 首板类策略 (8个)
| 文件 | 说明 |
|------|------|
| `01 首板低开策略.txt` | wywy1995原版首板低开 |
| `04 首板低开策略.txt` | 首板低开副本 |
| `34 韶华研究之十八 首板低开201系列.txt` | 首板低开201系列 |
| `68 wywy1995 首板低开策略代码优化，提快速度.txt` | 首板低开速度优化版 |
| `78 首板低开策略-终极版 最大回撤15%，年化50%.txt` | 首板低开终极版 |
| `13 首板高开-低开-弱转强混合策略（今年收益1138.61%）.txt` | 三合一混合策略 |
| `07 追首板涨停 过去两年年化304%.txt` | 追首板涨停 |
| `88 打首板策略，今年收益达到40%，绝无未来函数.txt` | 打首板策略 |

### 龙回头类策略 (3个)
| 文件 | 说明 |
|------|------|
| `01 龙回头3.0回测速度优化版.txt` | 龙回头3.0速度优化 |
| `89 2020年效果很好的策略-龙回头策略v3.0.txt` | 龙回头v3.0原版 |
| `99最新龙回头5.0速度优化版+风险控制版本.txt` | 龙回头5.0风控版 |

### 连板龙头类策略 (3个)
| 文件 | 说明 |
|------|------|
| `02 连板龙头策略.txt` | wywy1995连板龙头 |
| `07 连板龙头策略.txt` | 连板龙头副本 |
| `13 【涨停研究三】连板股票收益统计与回测.txt` | 连板统计研究 |

### 冲天炮类策略 (2个)
| 文件 | 说明 |
|------|------|
| `20 冲天炮最高板策略，收益惊呆了我.txt` | 冲天炮原版 |
| `95 冲天炮最高板策略迭代.txt` | 冲天炮迭代版 |

### 分歧反包类策略 (3个)
| 文件 | 说明 |
|------|------|
| `06 87.5%胜率之分歧反包二板.txt` | 分歧反包二板 |
| `14 胜率65%之缩量分歧反包战法.txt` | 缩量分歧反包 |
| `33 胜率88.9%之君正集团策略-大阳分歧反包.txt` | 大阳分歧反包 |

### 弱转强类策略 (3个)
| 文件 | 说明 |
|------|------|
| `21 【原创】年化123%最大回撤14%涨停弱转强竞价战法.txt` | 弱转强竞价战法 |
| `77 龙头弱转强战法.txt` | 龙头弱转强 |
| `13 首板高开-低开-弱转强混合策略.txt` | 混合策略中的弱转强 |

### 集合竞价类策略 (4个)
| 文件 | 说明 |
|------|------|
| `04 集合竞价摸奖策略1.0-致敬2022.txt` | 集合竞价摸奖 |
| `16 集合竞价三合一，今年收益1067%，年化198%.txt` | 三合一竞价策略 |
| `44 集合竞价摸奖策略1.0-致敬2022.txt` | 竞价摸奖副本 |
| `96 集合竞价量比策略V1.txt` | 竞价量比策略 |

### 龙头战法类策略 (6个)
| 文件 | 说明 |
|------|------|
| `02 龙头底分型战法-两年23倍.txt` | 龙头底分型 |
| `10 龙头首阴战法改版二.txt` | 龙头首阴 |
| `52 龙头战法之单阳不破.txt` | 单阳不破 |
| `56 大盘一路向上时，追总龙头2个月3倍.txt` | 追总龙头 |
| `86 最新解密--追涨龙头打板策略.txt` | 追涨龙头打板 |
| `88 WY大神的"龙头打板"策略小改.txt` | 龙头打板小改 |
| `94 布林线追龙头战法.txt` | 布林线追龙头 |

### 二板/排板类策略 (2个)
| 文件 | 说明 |
|------|------|
| `10 10倍-二板排板战法优化.txt` | 二板排板优化 |
| `55 怎么让龟速变奔跑？以"首版突破一进二"为例.txt` | 一进二研究 |

### 炸板类策略 (1个)
| 文件 | 说明 |
|------|------|
| `50 昨日炸板股策略.txt` | 昨日炸板反包 |

### 其他短线策略 (5个)
| 文件 | 说明 |
|------|------|
| `08 14个月200%，超短线实盘交易策略！无未来函数.txt` | 超短线实盘 |
| `32 追高概率涨停策略, 2022年化350%.txt` | 追高概率涨停 |
| `58 韶华研究之四-菲阿里四阶在T+1的短线策略应用.txt` | T+1短线 |
| `80 短线策略-2022年两个半月收益45%-无未来函数.txt` | 短线策略 |
| `94 小资金短线策略.txt` | 小资金短线 |

---

## 七、核心代码模式速查

### 涨停判断
```python
# 收盘涨停
df['close'] == df['high_limit']

# 曾涨停（触及涨停）
df['high'] == df['high_limit']

# 炸板（曾涨停但未封住）
(df['high'] == df['high_limit']) & (df['close'] != df['high_limit'])

# 一字涨停
df['low'] == df['high_limit']

# 非一字涨停
(df['close'] == df['high_limit']) & (df['low'] < df['high_limit'])
```

### 连板计算
```python
def get_continue_count_df(hl_list, date, watch_days):
    df = pd.DataFrame()
    for d in range(2, watch_days+1):
        HLC = get_hl_count_df(hl_list, date, d)
        CHLC = HLC[HLC['count'] == d]
        df = df.append(CHLC)
    # 取最大连板数
    ccd = pd.DataFrame()
    for s in set(df.index):
        tmp = df.loc[[s]]
        if len(tmp) > 1:
            M = tmp['count'].max()
            tmp = tmp[tmp['count'] == M]
        ccd = ccd.append(tmp)
    return ccd.sort_values(by='count', ascending=False)
```

### 相对位置计算
```python
def get_relative_position_df(stock_list, date, watch_days):
    df = get_price(stock_list, end_date=date, fields=['high','low','close'], 
                   count=watch_days, panel=False).dropna()
    close = df.groupby('code').apply(lambda df: df.iloc[-1,-1])
    high = df.groupby('code').apply(lambda df: df['high'].max())
    low = df.groupby('code').apply(lambda df: df['low'].min())
    result = pd.DataFrame()
    result['rp'] = (close - low) / (high - low)
    return result
```

### 集合竞价数据获取
```python
auction_data = get_call_auction(
    stock_code, 
    start_date="2024-01-01 09:15:00", 
    end_date="2024-01-01 09:26:00", 
    fields=['time', 'volume', 'current']
)
# current: 竞价匹配价格
# volume: 竞价匹配量
```

### 标准过滤函数
```python
def filter_kcbj_stock(initial_list):
    return [s for s in initial_list 
            if s[0] not in ('4', '8') and s[:2] != '68']

def filter_st_stock(initial_list, date):
    df = get_extras('is_st', initial_list, start_date=date, end_date=date, df=True)
    df = df.T
    return list(df[df['is_st'] == False].index)

def filter_paused_stock(initial_list, date):
    df = get_price(initial_list, end_date=date, frequency='daily', 
                   fields=['paused'], count=1, panel=False, fill_paused=True)
    return list(df[df['paused'] == 0].code)

def filter_new_stock(initial_list, date, days=50):
    return [s for s in initial_list 
            if (date - get_security_info(s).start_date).days > days]
```
