# 任务 04：消融实验矩阵与模块开关

你正在定义“去掉哪个模块会发生什么”的验证机制，不是在继续往策略里堆模块。

## 硬约束

1. 主结果必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_validation_tasks_20260403/result_04_ablation_matrix_and_module_switches.md`
2. 简版回执必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_validation_tasks_20260403/dispatch_prompts_20260403/results/04_任务_04_消融实验矩阵与模块开关_回执.md`
3. 必须覆盖以下模块族：
   - universe
   - alpha/signals
   - confirmation
   - risk
   - portfolio/weight
   - execution assumptions
4. 必须区分“增量加入测试”和“移除模块测试”两类消融。
5. 不要把本任务写成参数搜索。

## 必看材料

- `/Users/fengzhi/Downloads/git/testlixingren/docs/universal_mechanisms/32_master_portfolio_assembly.md`
- `/Users/fengzhi/Downloads/git/testlixingren/docs/universal_mechanisms/35_enhancement_replay_checklist.md`
- `/Users/fengzhi/Downloads/git/testlixingren/docs/universal_mechanisms/37_signal_confirmation_interface.md`
- `/Users/fengzhi/Downloads/git/testlixingren/docs/universal_mechanisms/39_strategy_factory_execution_checklist.md`

## 任务目标

为 `ablation/` 设计一套统一的模块开关与消融实验矩阵，确保后续能回答：到底是哪一层在贡献收益、降低回撤或减少误交易。

## 必须回答

1. 哪些模块必须支持开关化。
2. 默认的消融顺序应该是什么。
3. 模块之间有依赖时如何避免非法组合。
4. 消融结果该汇总成什么字段。
5. 哪些策略类型不适合跑全量消融。

## 必须产出

1. `ablation_matrix.csv` 建议字段。
2. 模块开关命名规范，例如 `enable_router`、`enable_confirmation`、`enable_exit_guard`。
3. 默认增量实验序列。
4. 默认移除实验序列。
5. 非法组合与依赖约束表。

## 结果文档至少包含

- 模块开关总表
- 消融矩阵模板
- 依赖与互斥规则
- 结果汇总结构
- 适配不同 profile 的建议
- 第一版最小实现建议

## 回执必须写明

- 你认为必须支持开关化的模块
- 你推荐的 5 个默认消融实验
- 最危险的非法组合
- 是否可以直接进入 runner 设计

## 通过门槛

- 后续任何策略都能复用同一套消融语言
- 能区分“模块有效”与“参数偶然”
- 不会因为组合依赖混乱导致结论失真