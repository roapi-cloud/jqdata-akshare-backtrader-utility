# 任务 03：`V0/V1/V2/V3` 基线矩阵

你正在固化统一对照矩阵，不是在做新的策略调参。

## 硬约束

1. 主结果必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_validation_tasks_20260403/result_03_baseline_matrix_v0_v1_v2_v3.md`
2. 简版回执必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_validation_tasks_20260403/dispatch_prompts_20260403/results/03_任务_03_V0_V1_V2_V3基线矩阵_回执.md`
3. 必须严格承接 `33_master_validation_pipeline.md` 中的 `V0/V1/V2/V3` 概念。
4. 必须给出每个版本的最小输入、最小输出、允许差异和禁止差异。
5. 不能只写四个名字，必须把四版到底怎么落地写清楚。

## 必看材料

- `/Users/fengzhi/Downloads/git/testlixingren/docs/universal_mechanisms/33_master_validation_pipeline.md`
- `/Users/fengzhi/Downloads/git/testlixingren/docs/universal_mechanisms/35_enhancement_replay_checklist.md`
- `/Users/fengzhi/Downloads/git/testlixingren/docs/universal_mechanisms/36_data_benchmark_cost_spec.md`

## 任务目标

把 `V0/V1/V2/V3` 从文档概念，固化成 `baseline_matrix/` 可直接执行的矩阵规范。

## 必须回答

1. `V0` 与 `V1` 的边界到底是什么。
2. `V2` 相对 `V1` 最少新增哪些增强层。
3. `V3` 何时必须接完整执行底座。
4. 哪些策略可以只跑 `基线版 / 增强版 / 压力版 / OOS版` 的简化矩阵。
5. 矩阵结果应该如何统一命名和汇总。

## 必须产出

1. `baseline_matrix.csv` 的字段设计。
2. `V0/V1/V2/V3` 的实验定义表。
3. 版本间只允许变化的参数清单。
4. 版本间禁止同时变化的参数清单。
5. 一份最小执行顺序：先跑什么，再跑什么，失败后怎么截断。

## 结果文档至少包含

- 四个版本的定义
- 输入输出 contract
- 参数冻结规则
- 简化矩阵适用条件
- 汇总表格式
- 常见误判与防错

## 回执必须写明

- `V0/V1/V2/V3` 的一句话定义
- 哪些参数在版本比较中必须冻结
- 你建议的 `baseline_matrix.csv` 核心字段
- 是否已达到“可以直接实现矩阵 runner”

## 通过门槛

- 不同人按你的文档跑，结果可比较
- 不会再出现“多个东西一起变了却当成单点增强”的情况
- 可以直接用于统一报告