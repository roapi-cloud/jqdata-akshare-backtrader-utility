# 任务 02：抽交易日历与基础池选择器

你正在做交易日历和基础池入口抽取，不是在研究某个策略具体买什么。

## 硬约束

1. 主结果必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_extraction_tasks_20260403/result_02_extract_trade_calendar_and_universe_selector.md`
2. 简版回执必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_extraction_tasks_20260403/dispatch_prompts_20260403/results/02_任务_02_抽交易日历与基础池选择器_回执.md`
3. 必须同时参考两边已有交易日、指数成分、全A入口实现。
4. 目标归宿限定为：`/Users/fengzhi/Downloads/git/testlixingren/strategy_kits/universe/trade_calendar/`
5. 允许直接创建最小骨架文件。

## 重点参考源

- 聚宽侧与 `get_trade_days`、`get_index_stocks`、`get_all_securities` 相关实现
- QuantsPlaybook 中与 `trade_days`、`trade_cal`、指数/标的池选择相关实现

## 任务目标

让新策略可以先选“全A / 指数 / ETF池 / 行业池”，并快速拿到调仓日、前一交易日、滚动窗口起止点。

## 必须产出

1. 统一基础池选择接口。
2. 统一交易日历接口。
3. 推荐目标文件拆分。
4. 如边界清晰，直接补最小骨架文件。

## 最小接口要求

至少定义清楚：

- `get_trade_days(start, end, market)`
- `shift_trade_day(date, n)`
- `resolve_base_universe(universe_type, date, config)`
- `get_rebalance_dates(start, end, freq, anchor)`

## 不该抽的内容

- 某个策略专属 ETF 白名单
- 某个研报专属行业池
- 某个具体策略的调仓时间点

## 通过门槛

- 新策略一开始就能统一拿交易日和基础池
- 不再在各策略里重复处理调仓日逻辑