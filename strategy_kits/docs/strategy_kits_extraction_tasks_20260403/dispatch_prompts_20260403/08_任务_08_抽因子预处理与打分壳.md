# 任务 08：抽因子预处理与打分壳

你正在做多因子评分壳抽取，不是在照搬某个 RFScore 或 ML 因子策略。

## 硬约束

1. 主结果必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_extraction_tasks_20260403/result_08_extract_factor_preprocess_and_scoring_shell.md`
2. 简版回执必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_extraction_tasks_20260403/dispatch_prompts_20260403/results/08_任务_08_抽因子预处理与打分壳_回执.md`
3. 必须同时参考：
   - 聚宽侧多因子预处理相关实现
   - QuantsPlaybook 中因子 pipeline / workflow 相关实现
4. 目标归宿限定为：`/Users/fengzhi/Downloads/git/testlixingren/strategy_kits/signals/factor_preprocess/`
5. 允许直接补最小骨架文件。

## 任务目标

让一个新多因子策略可以快速完成：原始因子表 -> 清洗 -> 标准化 -> 打分 -> 排序，而不必从头写完整流水线。

## 必须产出

1. 第一版需要保留的预处理步骤。
2. 打分壳最小接口。
3. 目标文件拆分建议。
4. 如边界清晰，直接补骨架文件。

## 最小接口要求

至少定义清楚：

- `fill_missing_by_group(...)`
- `winsorize_features(...)`
- `standardize_features(...)`
- `build_score_frame(...)`
- 输出：`score_df`

## 不该抽的内容

- 某个策略的具体因子列表
- 某个模型最终超参数
- 某个标签定义答案

## 通过门槛

- 新多因子策略能快速从因子表进入打分排序
- 通用预处理与策略专属因子选择边界清楚