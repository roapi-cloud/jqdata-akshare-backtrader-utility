# 任务 03：抽数据网关适配层

你正在做多数据源适配层抽取，不是在重写某个平台整套 SDK。

## 硬约束

1. 主结果必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_extraction_tasks_20260403/result_03_extract_data_gateways.md`
2. 简版回执必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_extraction_tasks_20260403/dispatch_prompts_20260403/results/03_任务_03_抽数据网关适配层_回执.md`
3. 必须同时参考：聚宽侧 JQ 风格接口封装、QuantsPlaybook 里的 TuShare / Qlib provider。
4. 目标归宿限定为：`/Users/fengzhi/Downloads/git/testlixingren/strategy_kits/execution/data_gateways/`
5. 本任务强调“最小可用适配层”，不要做全量平台能力。

## 重点参考源

- `聚宽有价值策略558/backtrader_base_strategy.py`
- `聚宽有价值策略558/test_jqdata_api.py`
- QuantsPlaybook 中与 `tushare_api.py`、`qlib_data_provider.py`、`data_provider.py` 相关文件

## 任务目标

让新策略能通过统一接口获取：行情、基础信息、财务/因子、交易约束、指数成分。

## 必须产出

1. 数据网关最小能力表。
2. `JQ / TuShare / Qlib` 对应接口映射表。
3. 统一 gateway interface。
4. 缓存与容错的最小要求。
5. 如边界清晰，直接补最小骨架文件。

## 最小接口要求

至少定义清楚：

- `get_price(...)`
- `get_fundamentals(...)`
- `get_all_securities(...)`
- `get_index_members(...)`
- `get_trade_days(...)`
- `get_extras(...)`

## 不该抽的内容

- 某个策略专属字段拼接逻辑
- 某个专题研究专属 feature engineering

## 通过门槛

- 新策略可以先写策略逻辑，再决定数据源
- 数据口径切换不再影响上层策略写法