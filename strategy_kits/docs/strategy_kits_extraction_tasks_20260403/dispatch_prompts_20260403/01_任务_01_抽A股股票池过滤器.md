# 任务 01：抽 A 股股票池过滤器

你正在做可复用股票池过滤器抽取，不是在整理某个单策略的选股逻辑。

## 硬约束

1. 主结果必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_extraction_tasks_20260403/result_01_extract_stock_pool_filters.md`
2. 简版回执必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_extraction_tasks_20260403/dispatch_prompts_20260403/results/01_任务_01_抽A股股票池过滤器_回执.md`
3. 必须同时参考：
   - `/Users/fengzhi/Downloads/git/testlixingren/聚宽有价值策略558`
   - `/Users/fengzhi/Downloads/git/testlixingren/QuantsPlaybook`
4. 目标归宿限定为：`/Users/fengzhi/Downloads/git/testlixingren/strategy_kits/universe/stock_pool_filters/`
5. 允许直接创建最小骨架文件，但不要顺手做因子打分或回测逻辑。

## 重点参考源

- `聚宽有价值策略558` 中所有与以下关键词相关的实现：
  - `filter_st_stock`
  - `filter_paused_stock`
  - `filter_limitup_stock`
  - `filter_limitdown_stock`
  - `filter_new_stock`
  - `get_stock`
- `QuantsPlaybook` 中：
  - `BuildStockPool.py`
  - 其他股票池/过滤器相关实现

## 任务目标

抽出一套新策略第一天就能直接复用的 A 股股票池过滤器。

## 必须产出

1. 源文件 -> 目标 kit 映射表。
2. 第一版要支持的过滤器清单，至少包括：
   - ST
   - 停牌
   - 次新
   - 涨停不可买
   - 跌停不可卖/不可买边界说明
   - 科创 / 北交所 / 创业板是否可配
   - 低流动性（如已有明确实现）
3. 统一输入输出 contract。
4. 推荐目标文件拆分。
5. 如果边界清晰，直接补最小骨架文件。

## 最小接口要求

至少定义清楚：

- 输入：`date`、`base_universe`、`config`
- 输出：
  - `filtered_universe`
  - `filter_stats`
  - `removed_reasons`

## 不该抽的内容

- 具体策略的最终股票数
- 具体 benchmark
- 某个策略特有的候选排序规则

## 结果文档至少包含

- 可复用过滤器列表
- 来源代码位置
- 目标目录与目标文件
- 最小接口
- 推荐默认配置
- 不应抽的边界
- 第一版实现顺序

## 回执必须写明

- 第一版立即实现的过滤器
- 你建议创建的目标文件名
- 最关键的输入输出字段
- 哪些部分要保留在策略层
- 是否已达到可实现状态

## 通过门槛

- 新策略能直接复用股票池过滤器
- 不再需要从 notebook 复制过滤代码
- 边界与责任足够清楚