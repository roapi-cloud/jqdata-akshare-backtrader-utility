# 任务 09：抽下单安全壳与持仓状态机

你正在做执行真实性相关抽取，不是在把某个策略的仓位答案硬编码为公共逻辑。

## 硬约束

1. 主结果必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_extraction_tasks_20260403/result_09_extract_order_safety_and_position_state.md`
2. 简版回执必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/strategy_kits_extraction_tasks_20260403/dispatch_prompts_20260403/results/09_任务_09_抽下单安全壳与持仓状态机_回执.md`
3. 必须同时参考：
   - 聚宽侧 `trade_tool.py`
   - QuantsPlaybook 中持仓对象 / T+1 状态机相关实现
4. 目标归宿限定为：
   - `strategy_kits/execution/order_safety/`
   - `strategy_kits/portfolio/position_state/`
   - `strategy_kits/portfolio/runtime_state/`
   - `strategy_kits/risk/stop_loss_hooks/`
5. 允许直接补最小骨架文件。

## 任务目标

让策略研究结果更容易落到真实交易约束，包括：安全下单、状态持久化、可卖数量、锁仓数量、止损挂钩。

## 必须产出

1. 可抽取执行安全能力列表。
2. 可抽取持仓状态能力列表。
3. 统一对象模型建议。
4. 如边界清晰，直接补骨架文件。

## 最小接口要求

至少定义清楚：

- `safe_order(...)`
- `order_target_value_safe(...)`
- `RuntimeState`
- `PositionState`
- `PortfolioState`
- `apply_stop_hook(...)`

## 不该抽的内容

- 某个策略的固定底仓比例
- 某个策略的固定止损阈值答案
- 某个策略特有的时点逻辑

## 通过门槛

- 新策略更容易接近真实执行
- 状态管理不再散落在单策略目录中