# 多策略组合方法总结

## 一、背景与动机

### 1.1 问题
很多量化策略采用"排序选Top N"的方式，如固定选前10只股票。这种做法存在以下问题：
- 股票池大小变化时，固定数量可能不合适
- 单一因子容易失效
- 没有充分利用更多信息

### 1.2 优化思路
将"固定数量选股"改为"百分比选股"，并在初筛池中叠加其他策略：
```
全市场 → 初筛20% → 精选10% → 最终持仓
```

---

## 二、多策略组合的四种方法（概念篇）

> **说明**：本章侧重讲解各方法的**核心原理与适用场景**，代码仅为最小化示意。
> 完整的工程实现（含数据泄露防护、边界处理、滚动回测框架等）见 **第四章**。

### 2.1 分层选股（Sequential Filtering）

**原理**：逐层筛选，每层使用不同因子

**代码示例**：
```python
# 第一层：用因子A选30%
pool1 = df.sort_values('factorA', ascending=False).head(int(0.3 * len(df)))

# 第二层：在pool1中用因子B选50%（相对于pool1，最终约15%）
# ⚠️ 注意：这里用 len(pool1) 而非 len(df)，否则比例会相对于全市场计算
pool2 = pool1.sort_values('factorB', ascending=False).head(int(0.5 * len(pool1)))

# 若希望最终保留全市场的10%，可反推第二层比例：
# 第二层比例 = 0.1 / 0.3 ≈ 0.33，即 pool1.head(int(0.33 * len(pool1)))
```

**优点**：
- 逻辑清晰，易于理解和调试
- 避免不同因子之间的直接冲突
- 每层可以使用不同类型的因子

**缺点**：
- 每层丢弃信息，可能误杀好股票
- 层数过多可能导致样本过少
- 因子顺序影响结果

**适用场景**：
- 因子之间有明显的先后逻辑关系
- 需要逐步缩小选股范围

**代码库示例**：
- `聚宽有价值策略558/71 多因子模板分位法策略.txt`

---

### 2.2 加权打分法（Weighted Scoring）

**原理**：多因子标准化后等权或加权求和

**代码示例**：
```python
from sklearn.preprocessing import StandardScaler

# 标准化
scaler = StandardScaler()
df[['value_std', 'momentum_std', 'quality_std']] = scaler.fit_transform(
    df[['value_factor', 'momentum_factor', 'quality_factor']]
)

# 加权打分
df['score'] = (df['value_std'] * 0.3 + 
               df['momentum_std'] * 0.3 + 
               df['quality_std'] * 0.2 + 
               df['growth_std'] * 0.2)

# 选股
selected = df.sort_values('score', ascending=False)[:int(0.1*len(df))]
```

**优点**：
- 保留全部信息，不丢失股票
- 简单直观，易于实现
- 可以灵活调整权重

**缺点**：
- 权重需要优化，可能过拟合
- 不同因子可能相互抵消
- 标准化方法影响结果

**权重优化方法**：
1. 等权重：各因子25%
2. 历史IC加权：按因子IC值分配权重
3. 机器学习优化：用模型学习最优权重
4. 市场状态动态调整

---

### 2.3 集成学习法（Ensemble Methods）

**原理**：多个模型/策略投票或加权融合

**代码示例**：
```python
from sklearn.ensemble import RandomForestClassifier, GradientBoostingClassifier
from xgboost import XGBClassifier
from lightgbm import LGBMClassifier

# 训练多个模型
rf = RandomForestClassifier(n_estimators=200)
gb = GradientBoostingClassifier(n_estimators=150)
xgb = XGBClassifier(n_estimators=200)
lgb = LGBMClassifier(n_estimators=200)

# 预测概率
rf_prob = rf.predict_proba(X)[:, 1]
gb_prob = gb.predict_proba(X)[:, 1]
xgb_prob = xgb.predict_proba(X)[:, 1]
lgb_prob = lgb.predict_proba(X)[:, 1]

# 加权融合
final_score = (rf_prob * 0.4 + 
               gb_prob * 0.3 + 
               xgb_prob * 0.15 + 
               lgb_prob * 0.15)

# 选股
selected = df[final_score > threshold]
```

**优点**：
- 提升稳定性，降低过拟合风险
- 综合多个模型的优势
- 适合复杂的非线性关系

**缺点**：
- 复杂度高，需要更多计算资源
- 模型选择和权重优化困难
- 可解释性较差

**典型实现与注意事项**：
- 模型组合示例：随机森林(40%) + Gradient Boosting(30%) + XGBoost(15%) + LightGBM(15%)
- **标签定义（y值）必须明确**：常见做法包括：
  - 二分类：未来N日收益率 > 0 记为1（涨），否则为0（跌）
  - 超额收益分类：未来N日收益率 - 基准收益率 > 阈值 记为1
  - 多分类：按收益分位数划分（如前30%/中间40%/后30%）
- **性能指标需结合具体环境解读**：
  - 文献中"AUC约0.55-0.65、准确率55%-60%"多见于A股全市场日频数据、二分类标签（次日涨跌）、2018-2023年回测
  - 该数字**不是普适结论**，实际表现高度依赖：数据集覆盖范围、回测时间段、市场牛熊阶段、因子质量、标签定义
  - A股存在涨跌停限制、T+1交易、印花税等摩擦成本，模型输出需结合实际交易规则评估
- **必须使用滚动训练/时间序列交叉验证**，严禁随机shuffle分割，否则引入未来函数

---

### 2.4 动态权重法（Market Regime Adaptive）

**原理**：根据市场状态调整因子权重

**代码示例**：
```python
def get_market_state(df):
    """判断市场状态
    
    ⚠️ 警告：以下仅为教学示例，存在严重局限性：
    1. 20日窗口过短，容易受短期噪音干扰
    2. 简单涨跌阈值无法识别震荡市、结构性行情
    3. 存在明显滞后性：当确认"牛市"时可能已接近顶部
    4. 单一指标（沪深300收益率）信息量不足
    实盘中建议使用多维度信号组合，见下方"改进建议"
    """
    # 简化示例：用沪深300近20日涨跌判断
    hs300_return = df['hs300_close'].pct_change(20).iloc[-1]
    if hs300_return > 0.05:
        return 'bull'
    elif hs300_return < -0.05:
        return 'bear'
    else:
        return 'neutral'

def get_weights(market_state):
    """根据市场状态获取权重"""
    if market_state == 'bull':
        return {'momentum': 0.4, 'growth': 0.4, 'value': 0.2, 'quality': 0.0}
    elif market_state == 'bear':
        return {'value': 0.4, 'quality': 0.4, 'momentum': 0.2, 'growth': 0.0}
    else:
        return {'value': 0.25, 'momentum': 0.25, 'quality': 0.25, 'growth': 0.25}

# 使用
market_state = get_market_state(df)
weights = get_weights(market_state)
df['score'] = sum(df[f'{k}_std'] * v for k, v in weights.items())
```

**优点**：
- 适应市场风格切换
- 牛市重进攻，熊市重防守
- 更符合投资逻辑

**缺点**：
- 市场状态判断困难
- 权重切换可能滞后
- 需要更多的历史数据

**⚠️ 市场状态判断的改进建议**：
- 不要依赖单一指标，建议组合使用：
  - 趋势类：均线排列（MA20/MA60/MA120）、MACD、布林带位置
  - 波动率类：VIX、ATR、历史波动率分位数
  - 量能类：成交额均线比、换手率分位数
  - 宏观类：PMI、社融增速、利率期限利差
  - 情绪类：融资融券余额、北向资金流向、涨跌家数比
- 使用隐马尔可夫模型（HMM）或聚类算法识别市场状态，比硬阈值更稳健
- 权重切换建议采用**平滑过渡**而非硬切换，例如用sigmoid函数映射权重，避免频繁跳变
- 必须做**样本外检验**：用某段时间定义的状态规则，在另一段时间验证有效性

---

## 三、因子有效性检验（IC值分析）

### 3.1 什么是IC值

IC（Information Coefficient）：因子值与下期收益率的相关系数

| IC值 | 含义 |
|------|------|
| IC > 0.03 | 因子有实用价值（需结合其他指标综合判断） |
| IC > 0.05 | 因子较强 |
| IC均值/IC标准差 > 2 | 因子稳定可靠 |

**IC值门槛的重要补充说明**：
- "IC > 0.03就有实用价值"是一个经验法则，但**不能孤立使用**，必须同时考察：
  1. **IC分布的稳定性**：单期IC高不代表稳定，需观察IC序列的标准差、偏度、正IC比例（IC>0的期数占比，>55%较理想）
  2. **市场环境差异**：同一因子在牛市/熊市/震荡市中的IC可能差异巨大，需分阶段检验
  3. **交易成本覆盖能力**：IC高不等于能盈利，高换手因子即使IC=0.05，扣除手续费和冲击成本后可能为负
  4. **因子衰减速度**：IC随持有期延长通常会衰减，需测试不同持有期（1日/5日/20日）的IC变化
  5. **截面相关性**：多个因子之间若高度相关，叠加后IC不会线性提升，需做正交化处理
- 综合评估指标：IC_IR（信息比率）= IC均值 / IC标准差，通常IC_IR > 0.5 才具备实盘价值

### 3.2 IC值计算

```python
def calculate_ic(factor_values, future_returns):
    """计算IC值"""
    import scipy.stats as stats
    ic, _ = stats.spearmanr(factor_values, future_returns)
    return ic

# 多期IC计算
ic_series = []
for date in rebalance_dates:
    ic = calculate_ic(df.loc[date, 'factor'], df.loc[date, 'future_return'])
    ic_series.append(ic)

ic_mean = np.mean(ic_series)
ic_std = np.std(ic_series)
ic_ir = ic_mean / ic_std  # IC信息比
```

### 3.3 分层回测

```python
def stratified_backtest(df, factor_col, n_groups=5):
    """分层回测验证因子有效性"""
    df['group'] = pd.qcut(df[factor_col], q=n_groups, labels=False)
    df['group'] = n_groups - df['group']  # 反转，第1组为最高分
    
    group_stats = df.groupby('group')['future_return'].agg(['mean', 'std', 'count'])
    
    # 多空收益
    long_short = group_stats.loc[1, 'mean'] - group_stats.loc[n_groups, 'mean']
    
    return group_stats, long_short
```

---

## 四、完整多因子选股系统（工程实现篇）

> **说明**：本章将第二章介绍的四种方法整合为**可运行的工程框架**，重点解决：
> - 截面标准化防止数据泄露
> - 分层选股比例计算的正确实现
> - 多日期面板数据的滚动处理
> - 选股数量下限保护
> 
> 与第二章的区别：第二章是"概念+最小示例"，本章是"生产可用代码"。

### 4.1 系统架构

```
数据获取 → 因子计算 → 因子检验 → 因子组合 → 选股 → 回测
   ↓           ↓          ↓          ↓        ↓       ↓
  OHLCV    价值/动量   IC分析    加权打分   Top N   收益率
  财务数据   质量/成长   分层回测   分层筛选   百分比   风险指标
```

### 4.2 代码框架

```python
class MultiFactorModel:
    """多因子选股模型"""
    
    def __init__(self, weights=None):
        self.weights = weights or {
            'value': 0.25,
            'momentum': 0.25,
            'quality': 0.25,
            'growth': 0.25
        }
    
    def calculate_factors(self, df):
        """计算各类因子"""
        # 价值因子
        df['value_factor'] = 1/df['PE'] + 1/df['PB']
        
        # 动量因子
        df['momentum_factor'] = df['close'].pct_change(250)
        
        # 质量因子
        df['quality_factor'] = df['ROE'] * 0.5 + df['gross_margin'] * 0.3 - df['debt_ratio'] * 0.2
        
        # 成长因子
        df['growth_factor'] = df['revenue_growth'] * 0.4 + df['profit_growth'] * 0.6
        
        return df
    
    def standardize_and_combine(self, df):
        """标准化并组合因子
        
        ⚠️ 数据泄露警告：
        如果 df 包含多个时间点的全量数据，直接 fit_transform 会用全量数据的均值/方差做标准化，
        相当于引入了未来函数（用到了未来数据的信息）。
        
        正确做法：按每个调仓日分别做截面标准化（cross-sectional standardization），
        即每个日期截面上，只使用该日期股票池内的均值和标准差。
        """
        factor_cols = ['value_factor', 'momentum_factor', 'quality_factor', 'growth_factor']
        
        # 安全做法：按日期分组做截面标准化
        if 'date' in df.columns:
            for col in factor_cols:
                df[f'{col}_std'] = df.groupby('date')[col].transform(
                    lambda x: (x - x.mean()) / x.std() if x.std() > 0 else 0
                )
            std_cols = [f'{col}_std' for col in factor_cols]
        else:
            # 如果df只包含单个日期的截面数据，可以直接标准化
            from sklearn.preprocessing import StandardScaler
            scaler = StandardScaler()
            df[factor_cols] = scaler.fit_transform(df[factor_cols])
            std_cols = factor_cols
        
        # 加权组合
        df['score'] = sum(df[col] * w for col, w in zip(std_cols, self.weights.values()))
        
        return df
    
    def select_stocks(self, df, method='percent', top_n=10, top_pct=0.1):
        """选股"""
        df = df.sort_values('score', ascending=False)
        
        if method == 'fixed':
            return df.head(top_n)
        elif method == 'percent':
            return df.head(int(len(df) * top_pct))
        else:
            raise ValueError("method must be 'fixed' or 'percent'")
    
    def layered_select(self, df, layers, mode='relative'):
        """分层选股
        
        Args:
            df: 股票池DataFrame
            layers: 列表，每个元素为 (因子名, 筛选比例)
                    示例: [('factor1', 0.3), ('factor2', 0.5)]
            mode: 比例计算模式
                - 'relative'（推荐）：每层比例相对于上一层池子，
                  如第一层30%、第二层50% => 最终为 30% * 50% = 15%
                - 'absolute'：每层比例相对于原始全市场，
                  如第一层30%、第二层10% => 最终为 10%
        
        ⚠️ 原代码bug修复：
        旧代码 `pool.head(int(len(df) * pct))` 始终用全市场长度计算，
        导致第二层比例不是相对于第一层池子，而是相对于全市场，
        这在 'relative' 语义下是错误的。
        """
        pool = df.copy()
        for factor, pct in layers:
            pool = pool.sort_values(factor, ascending=False)
            if mode == 'relative':
                # 相对于当前池子的比例
                pool = pool.head(int(len(pool) * pct))
            elif mode == 'absolute':
                # 相对于原始全市场的比例
                pool = pool.head(int(len(df) * pct))
            else:
                raise ValueError("mode must be 'relative' or 'absolute'")
        return pool
```

### 4.3 使用示例

```python
# 初始化模型
model = MultiFactorModel(weights={
    'value': 0.3,
    'momentum': 0.3,
    'quality': 0.2,
    'growth': 0.2
})

# 数据准备：df 必须包含 'date' 列（用于截面标准化）
# 以及各因子所需的原始字段：PE, PB, ROE, close, gross_margin, debt_ratio, revenue_growth, profit_growth

# 计算因子
df = model.calculate_factors(df)

# 标准化并组合（自动按date分组做截面标准化，防止数据泄露）
df = model.standardize_and_combine(df)

# 百分比选股（选10%，最少5只）
selected = model.select_stocks(df, method='percent', top_pct=0.1, min_stocks=5)

# 或者分层选股
# 注意：mode='relative' 时，第二层的0.33是相对于第一层池子的比例
# 最终保留约 30% * 33% ≈ 10% 的股票
selected = model.layered_select(df, layers=[
    ('value_factor', 0.3),    # 先用价值因子选30%
    ('momentum_factor', 0.33) # 再用动量因子选pool1的33%
], mode='relative')
```

### 4.4 滚动回测框架（防止未来函数）

```python
def rolling_backtest(df, model, rebalance_dates):
    """滚动回测框架：每个调仓日独立计算因子、标准化、选股
    
    这是防止未来函数的关键：每个日期只使用该日期及之前的数据
    """
    results = []
    
    for date in rebalance_dates:
        # 1. 获取当前日期的截面数据
        cross_section = df[df['date'] == date].copy()
        
        if len(cross_section) < 50:
            continue  # 股票池太小，跳过
        
        # 2. 计算因子（只用当期可用数据）
        cross_section = model.calculate_factors(cross_section)
        
        # 3. 截面标准化（只用当期截面数据，不泄露未来）
        cross_section = model.standardize_and_combine(cross_section)
        
        # 4. 选股
        selected = model.select_stocks(cross_section, method='percent', top_pct=0.1)
        
        # 5. 记录结果
        results.append({
            'date': date,
            'selected_stocks': selected['code'].tolist(),
            'pool_size': len(cross_section),
            'selected_count': len(selected)
        })
    
    return results
```

---

## 五、因子选择建议

### 5.1 不同市场环境的因子权重

| 市场环境 | 推荐因子组合 | 权重建议 |
|---------|-------------|---------|
| 价值投资 | 价值60% + 质量30% + 动量10% | 重价值轻动量 |
| 趋势跟踪 | 动量50% + 成长30% + 价值20% | 重动量轻价值 |
| 均衡配置 | 等权重四分法 | 各25% |
| 牛市环境 | 动量40% + 成长40% + 质量20% | 进攻型 |
| 熊市环境 | 价值40% + 质量40% + 动量20% | 防守型 |

### 5.2 常见因子类型

| 因子类别 | 代表指标 | 逻辑解释 |
|---------|---------|---------|
| 价值因子 | PE、PB、PCF | 低估股票长期会回归合理价值 |
| 动量因子 | 过去12个月收益率 | 趋势延续效应 |
| 质量因子 | ROE、毛利率、负债率 | 高质量公司抗风险能力强 |
| 成长因子 | 营收增速、利润增速 | 高成长公司估值提升 |
| 规模因子 | 市值 | 小盘股长期有超额收益 |

---

## 六、常见陷阱与注意事项

### 6.1 常见陷阱

1. **未来函数**：使用未来数据计算因子（如用年报发布后的数据）
2. **幸存者偏差**：只用当前存在的股票回测，忽略退市股票
3. **过拟合**：在历史数据上过度优化参数
4. **交易成本**：忽略手续费、冲击成本
5. **流动性风险**：小市值股票无法大额交易

### 6.2 注意事项

1. **股票池太小**：当股票池<50只时，百分比可能选太少，建议设下限
2. **因子相关性**：叠加策略时注意因子相关性，避免重复筛选同一逻辑
3. **标准化方法**：不同标准化方法（Z-Score、Min-Max、Robust）影响结果
4. **回测周期**：至少需要3-5年数据，覆盖牛熊周期
5. **调仓频率**：过频繁增加交易成本，过少可能错过机会

---

## 七、参考资源

### 7.1 代码库内资源

| 文件 | 说明 |
|------|------|
| `聚宽有价值策略558/71 多因子模板分位法策略.txt` | 分层选股实现 |
| `聚宽有价值策略558/79 多因子选股模型框架.txt` | 等权打分框架 |
| `聚宽有价值策略558/24 多因子线性回归组合策略.txt` | 线性回归组合 |
| `聚宽有价值策略558/97 稳定高回报周期股策略2-RSRS择时.txt` | 加权打分示例 |

### 7.2 外部参考

> **有效性说明**：以下链接在文档编写时可访问，但外部链接可能随时失效，建议及时保存关键内容。
> 各资源质量参差不齐，阅读时请注意甄别。

| 资源 | 链接 | 质量评估与说明 |
|------|------|---------------|
| QuantsPlaybook（券商研报复现） | https://github.com/hugo2046/QuantsPlaybook | ⭐⭐⭐⭐⭐ GitHub开源项目，系统性地复现券商金工研报，代码可运行，适合学习因子构建和回测框架 |
| 聪明钱因子双维度分层分析框架 | https://bigquant.com/wiki/doc/Ath4Umo467 | ⭐⭐⭐⭐ BigQuant平台官方文档，分层回测框架完整，但需注意平台特定API，迁移到其他环境需改造 |
| Python多因子量化选股模型全景实战 | https://juejin.cn/post/7615250753934737427 | ⭐⭐⭐ 掘金社区文章，覆盖面广但深度有限，代码片段需自行验证，适合作为入门概览 |
| 多策略集成学习选股模型技术实现(V7.3) | https://gitcode.csdn.net/69bcfd3854b52172bc62d9bf.html | ⭐⭐⭐ CSDN博客，集成学习实现有参考价值，但性能数据需结合具体数据集理解，不可直接套用 |
| 期货量化策略组合优化 | https://www.wsisp.com/helps/70829.html | ⭐⭐ 第三方网站，内容偏期货且可能与本文股票场景不完全匹配，链接有效性需自行验证 |

**推荐延伸阅读**：
- 《主动投资组合管理》（Grinold & Kahn）：量化投资经典教材，IC/IR理论的系统阐述
- 《因子投资：方法与实践》（石川等）：国内因子投资实战指南，A股市场针对性强
- 国泰君安/中信建投等券商金工研报：Wind/Choice终端可查，因子研究和组合构建的一手资料

---

## 八、总结

### 8.1 方法对比

| 方法 | 复杂度 | 稳定性 | 信息保留 | 适用场景 |
|------|--------|--------|----------|----------|
| 分层选股 | 低 | 中 | 低 | 因子有先后逻辑 |
| 加权打分 | 低 | 中 | 高 | 均衡配置 |
| 集成学习 | 高 | 高 | 高 | 复杂非线性关系 |
| 动态权重 | 中 | 高 | 高 | 市场风格切换明显 |

### 8.2 推荐做法

1. **先用百分比替代固定数量**（如选前10%而非top10）
2. **叠加第二个因子做分层筛选**（注意比例是相对于上一层池子）
3. **用IC值检验因子有效性**（IC > 0.03是起点，还需看IC_IR、正IC比例、市场环境）
4. **做分层回测验证**（Top组收益 > Bottom组才是好因子，且需单调性）
5. **考虑交易成本和流动性**（高IC不等于高收益，换手率决定成本）
6. **严格按日期截面标准化**（防止数据泄露/未来函数）
7. **使用滚动训练/时间序列交叉验证**（严禁随机shuffle）

---

*文档创建时间：2026-03-31*
*基于代码库：jqdata_akshare_backtrader_utility*
