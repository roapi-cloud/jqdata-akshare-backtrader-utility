Append this after the shared dispatch header.

Goal:
Create example panel files and a storage note for the first three strategies.

Write scope:
1. `jq_workbench/research/panels/README.md`
2. `jq_workbench/research/panels/examples.md`

Inputs:
1. `jq_workbench/governance/standards/panel_contracts.md`
2. `jq_workbench/research/task_specs/*.json`

Work:
1. document example `pool_panel`, `score_panel`, and `prediction_frame` shapes
2. show naming, versioning, and lifecycle notes
3. include small inline CSV examples

Do not:
1. generate large datasets
2. modify validators

Done when:
1. future agents know what panel files should look like before implementation scripts are written
