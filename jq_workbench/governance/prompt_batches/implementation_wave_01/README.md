# Implementation Wave 01

This batch contains 20 prompts for the next execution phase after the workbench architecture draft.

Use each file as one agent assignment.

Recommended order:

1. Fix contract drift first: `01` to `04`
2. Add core examples and registries: `05` to `10`
3. Build JoinQuant execution scaffolding: `11` to `15`
4. Add governance and review assets: `16` to `20`

Common rule:

1. Copy the global header from `jq_workbench/governance/prompt_templates/shared_dispatch_header.md`
2. Then append one prompt file from this folder

Each prompt is self-contained and owns a narrow write scope.
