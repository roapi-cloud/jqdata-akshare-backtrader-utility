Append this after the shared dispatch header.

Goal:
Write the first concrete migration plan from legacy repository sprawl to the new mainline.

Write scope:
1. `jq_workbench/docs/migration_plan_wave_01.md`

Inputs:
1. `jq_workbench/docs/architecture.md`
2. `jq_workbench/README.md`
3. root repository structure

Work:
1. define what stays, what freezes, what gets referenced only
2. propose migration order for real files and future outputs

Do not:
1. move files
2. rename repository directories

Done when:
1. the migration path is explicit enough for future execution work
