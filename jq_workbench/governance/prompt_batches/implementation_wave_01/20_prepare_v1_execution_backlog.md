Append this after the shared dispatch header.

Goal:
Turn the current design package into a prioritized V1 implementation backlog.

Write scope:
1. `jq_workbench/docs/v1_execution_backlog.md`

Inputs:
1. all docs inside `jq_workbench/docs/`
2. all standards inside `jq_workbench/governance/standards/`

Work:
1. list implementation epics
2. split each epic into small buildable tasks
3. mark prerequisites, owners, and risk level

Do not:
1. write code
2. reopen settled architecture decisions

Done when:
1. the team can start execution without another planning pass
