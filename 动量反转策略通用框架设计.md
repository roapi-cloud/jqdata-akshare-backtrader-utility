# 动量/反转策略通用框架设计

## 一、策略类型概述

### 核心逻辑
- **动量效应（Momentum Effect）**：强者恒强，过去表现好的资产在未来一段时间内继续表现好。核心假设是趋势具有持续性。
- **反转效应（Reversal Effect）**：物极必反，过去表现差的资产在未来一段时间内会反弹，过去表现好的会回调。核心假设是市场存在过度反应。

### 适用场景
| 策略类型 | 适用市场环境 | 典型周期 |
|---------|------------|---------|
| 短期反转 | 震荡市、情绪极端时 | 1-5日 |
| 中期动量 | 趋势市、牛市 | 20-90日 |
| 长期反转 | 熊市末期、牛市末期 | 120-252日 |
| 行业动量/反转 | 板块轮动明显时 | 1-6月 |
| 指数动量轮动 | 大小盘切换、风格轮动 | 15-30日 |

### 风险特征
- 动量策略：在趋势延续时收益显著，但在趋势反转时回撤大（"动量崩溃"）
- 反转策略：在震荡市表现优异，但在强趋势市中容易被套
- 共同风险：高频调仓导致交易成本高、极端行情下流动性风险

---

## 二、子策略变体分类

### 2.1 价格动量类
| 变体 | 核心算法 | 代表文件 |
|-----|---------|---------|
| 简单收益率动量 | `(P_t - P_{t-N}) / P_{t-N}` | `59 年化62%的动量策略.txt` |
| 对数线性回归动量 | `slope * r_squared` | `88 基于动量因子的ETF轮动加上RSRS择时.txt` |
| 乖离率动量 | `(Price / MA_N - 1)` 的斜率 | `16 动量ETF轮动RSRS择时-升级.txt` |
| BBI动量 | `BBI / Price` | `69 宽基BBI动量追涨完美避开这波大跌.txt` |
| 斜率×拟合度动量 | `annualized_returns * r_squared` | `27 根据动量模型切换大小盘板块选股策略.txt` |

### 2.2 行业动量/反转类
| 变体 | 核心算法 | 代表文件 |
|-----|---------|---------|
| 行业宽度动量 | 行业内站上MA的股票占比 | `05 四大搅屎棍策略.txt` |
| 行业超额收益反转 | 短期+中期相关性预测 | `51 行业反转效应（年化32%，回撤8%）.txt` |
| 行业轮动 | 行业收益率排序 | `30 价值成长轮动策略.txt` |

### 2.3 指数动量轮动类
| 变体 | 核心算法 | 代表文件 |
|-----|---------|---------|
| 大小盘动量切换 | 指数斜率得分比较 | `27 根据动量模型切换大小盘板块选股策略.txt` |
| 多品种ETF动量 | 年化收益×判定系数 | `05 8年10倍回撤小,有滑点,ETF动量简单轮动策略.txt` |
| EPO优化动量 | 协方差收缩优化权重 | `17 多品种ETF动量轮动+EPO优化.txt` |

### 2.4 超跌反弹/均值回归类
| 变体 | 核心算法 | 代表文件 |
|-----|---------|---------|
| MA55突破反转 | 连续下跌后突破MA55 | `30 【超跌反弹v1.0】3年2.3倍的超跌反弹小牛股.txt` |
| 均线回踩反弹 | 回踩MA55后拉升 | `71 回踩均线搏反弹，21年来70%..txt` |
| TD趋势反转 | DeMark Setup/Count | `74 迪马克TD趋势反转指标.txt` |
| 30日反转效应 | 过去30日跌幅最大 | `67 基于动量和反转效应的沪深300成分股策略.txt` |

### 2.5 动量反转组合类
| 变体 | 核心算法 | 代表文件 |
|-----|---------|---------|
| 动量反转双效应 | 30日反转+70日动量 | `67 基于动量和反转效应的沪深300成分股策略.txt` |
| 大小盘反复横跳 | 20日指数动量切换风格 | `39 干积分-大小盘反复横跳V2.0.txt` |
| 价值成长轮动 | 指数动量决定风格 | `30 价值成长轮动策略.txt` |
| 此消彼长 | 多因子+大小盘组合 | `45 此消彼长.txt` |

---

## 三、通用代码框架

### 3.1 核心模块定义

#### 模块1: 动量计算器 (MomentumCalculator)
```python
class MomentumCalculator:
    """动量因子计算模块，支持多种动量计算方法"""
    
    def get_price_momentum(self, stock_list: List[str], date: str, 
                           periods: List[int] = [5, 10, 20, 60]) -> pd.DataFrame:
        """价格动量(N日收益率)
        
        核心公式: momentum = (P_t - P_{t-N}) / P_{t-N}
        
        适用场景: 简单动量筛选、ETF轮动
        代表策略: 59 年化62%的动量策略
        """
        pass
    
    def get_slope_momentum(self, stock_list: List[str], date: str, 
                           period: int = 20) -> pd.Series:
        """斜率动量(线性回归斜率×判定系数)
        
        核心公式:
            y = log(close)
            x = arange(len(y))
            slope, intercept = polyfit(x, y, 1)
            r_squared = 1 - SS_res / SS_tot
            score = annualized_returns * r_squared
        
        适用场景: ETF轮动、指数风格切换
        代表策略: 88 基于动量因子的ETF轮动加上RSRS择时
        """
        pass
    
    def get_bias_momentum(self, stock_list: List[str], date: str, 
                          bias_period: int = 90, momentum_period: int = 10) -> pd.Series:
        """乖离率动量
        
        核心公式:
            bias = Price / MA(bias_period) - 1
            momentum = polyfit(bias序列, 1).slope
        
        适用场景: 趋势强度判断
        代表策略: 16 动量ETF轮动RSRS择时-升级
        """
        pass
    
    def get_risk_adjusted_momentum(self, stock_list: List[str], date: str, 
                                    period: int = 20) -> pd.Series:
        """风险调整动量(夏普比率)
        
        核心公式: sharpe = mean(returns) / std(returns) * sqrt(250)
        
        适用场景: 需要控制波动的动量策略
        """
        pass
    
    def get_sector_momentum(self, sectors: List[str], date: str, 
                            period: int = 20) -> pd.Series:
        """行业动量(行业宽度)
        
        核心公式: 
            行业内站上MA的股票占比 = count(price > MA) / total_stocks
        
        适用场景: 行业轮动、四大搅屎棍策略
        代表策略: 05 四大搅屎棍策略
        """
        pass
    
    def get_index_momentum(self, index_list: List[str], date: str,
                           period: int = 20) -> Dict[str, float]:
        """指数动量(用于大小盘切换)
        
        核心公式: score = annualized_returns * r_squared
        
        适用场景: 大小盘风格切换
        代表策略: 27 根据动量模型切换大小盘板块选股策略
        """
        pass
```

#### 模块2: 反转计算器 (ReversalCalculator)
```python
class ReversalCalculator:
    """反转因子计算模块"""
    
    def get_short_term_reversal(self, stock_list: List[str], date: str, 
                                 period: int = 5) -> pd.Series:
        """短期反转(5日反转)
        
        核心公式: reversal = -(P_t - P_{t-N}) / P_{t-N}
        
        适用场景: 超跌反弹、短期均值回归
        代表策略: 67 基于动量和反转效应的沪深300成分股策略
        """
        pass
    
    def get_long_term_reversal(self, stock_list: List[str], date: str, 
                                period: int = 252) -> pd.Series:
        """长期反转(年反转)
        
        核心公式: reversal = -(P_t - P_{t-252}) / P_{t-252}
        
        适用场景: 年度级别均值回归
        """
        pass
    
    def get_ma_reversal(self, stock_list: List[str], date: str, 
                        ma_period: int = 55) -> pd.Series:
        """均线突破反转
        
        核心逻辑: 连续下跌后首次突破MA55
        买入条件:
            1. 近40天内站上MA55的天数 ≤ 3
            2. 近3天内有站上MA55
            3. 近10天最低价是近60天最低
        卖出条件:
            1. 跌破MA5
            2. 单日下跌5%
            3. 高点回撤5%
            4. 总亏损5%
        
        适用场景: 超跌反弹策略
        代表策略: 30 【超跌反弹v1.0】3年2.3倍的超跌反弹小牛股
        """
        pass
    
    def get_tidal_factor(self, stock_list: List[str], date: str) -> pd.Series:
        """潮汐因子(方正证券)
        
        核心逻辑: 量价配合的反转信号
        构建方法: 基于成交量激增时刻的价格行为
        """
        pass
    
    def get_industry_reversal(self, date: str, short_period: int = 2, 
                               long_period: int = 6) -> pd.Series:
        """行业反转效应
        
        核心逻辑:
            1. 计算各行业rolling月份超额收益
            2. 短期相关性 × 短期收益 = 短期预测
            3. 长期相关性 × 长期收益 = 长期预测
            4. 综合预测 = (短期 + 长期) / 2
        
        适用场景: 行业ETF轮动
        代表策略: 51 行业反转效应（年化32%，回撤8%）
        """
        pass
    
    def get_td_reversal(self, code: str, date: str, 
                        setup_period: int = 8, count_period: int = 13) -> Dict:
        """TD序列趋势反转
        
        核心逻辑:
            Setup阶段: 连续N天收盘价 < 4天前收盘价(下跌setup)
            Count阶段: 突破setup期间极值后开始计数
            交易信号: Count达到目标数后反转
        
        适用场景: 期货/指数趋势反转
        代表策略: 74 迪马克TD趋势反转指标
        """
        pass
```

#### 模块3: 动量反转组合器 (MomentumReversalCombiner)
```python
class MomentumReversalCombiner:
    """动量反转组合与择时模块"""
    
    def combine_momentum_reversal(self, momentum: pd.Series, reversal: pd.Series, 
                                   mom_weight: float = 0.5) -> pd.Series:
        """动量反转组合得分
        
        核心公式: score = momentum * mom_weight + reversal * (1 - mom_weight)
        
        适用场景: 同时利用动量和反转效应
        代表策略: 67 基于动量和反转效应的沪深300成分股策略
        """
        pass
    
    def regime_switching(self, momentum: pd.Series, reversal: pd.Series, 
                         market_regime: str) -> pd.Series:
        """根据市场状态切换动量/反转权重
        
        核心逻辑:
            - 牛市/趋势市: 动量权重 > 反转权重
            - 震荡市: 反转权重 > 动量权重
            - 熊市: 降低总仓位
        
        判断方法:
            1. RSRS择时: slope_zscore * r2 > threshold → 趋势
            2. MA择时: MA_short > MA_long → 趋势
            3. 指数动量: 大小盘相对强弱
        
        代表策略: 30 价值成长轮动策略
        """
        pass
    
    def get_rsrs_timing_signal(self, stock: str, date: str, 
                                N: int = 18, M: int = 600, 
                                threshold: float = 0.7) -> str:
        """RSRS择时信号
        
        核心公式:
            slope, intercept = polyfit(low, high, 1)
            r2 = 判定系数
            zscore = (slope - mean) / std
            rsrs_score = zscore * r2  (修正标准分)
            rsrs_score = zscore * r2 * slope  (右偏标准分)
        
        信号:
            rsrs_score > threshold → "BUY"
            rsrs_score < -threshold → "SELL"
            else → "KEEP"
        
        代表策略: 88 基于动量因子的ETF轮动加上RSRS择时
        """
        pass
    
    def get_ma_timing_signal(self, stock: str, date: str,
                              short_period: int = 20, diff_period: int = 3) -> str:
        """均线择时信号
        
        核心逻辑:
            MA_short = mean(close[-short_period:])
            MA_long = mean(close[-(short_period+diff_period):-diff_period])
            MA_short > MA_long → 趋势向上
        
        代表策略: 05 8年10倍回撤小,有滑点,ETF动量简单轮动策略
        """
        pass
```

### 3.2 框架配置类
```python
from dataclasses import dataclass, field
from typing import List, Dict, Optional

@dataclass
class MomentumReversalConfig:
    """动量/反转策略通用配置类"""
    
    # ========== 动量配置 ==========
    momentum_type: str = "slope"  # return / slope / bias / sharpe / sector
    momentum_periods: List[int] = field(default_factory=lambda: [5, 10, 20, 60])
    momentum_period: int = 20  # 单一动量周期
    bias_period: int = 90  # 乖离率计算周期(仅bias类型)
    momentum_direction: str = "asc"  # asc=买动量高的, desc=买动量低的(反转)
    
    # ========== 反转配置 ==========
    reversal_type: str = "short_term"  # short_term / long_term / ma_break / industry / td
    reversal_period: int = 5  # 短期反转周期
    use_long_term_reversal: bool = False
    long_term_reversal_period: int = 252
    ma_break_period: int = 55  # 均线突破周期
    td_setup_period: int = 8
    td_count_period: int = 13
    
    # ========== 组合配置 ==========
    use_combination: bool = False
    momentum_weight: float = 0.5
    use_regime_switching: bool = False
    regime_method: str = "rsrs"  # rsrs / ma / index_momentum
    
    # ========== 择时配置 ==========
    use_timing: bool = True
    timing_method: str = "rsrs_ma"  # rsrs_ma / rsrs_only / ma_only / none
    rsrs_N: int = 18
    rsrs_M: int = 600
    rsrs_threshold: float = 0.7
    ma_short_period: int = 20
    ma_diff_period: int = 3
    
    # ========== 股票池配置 ==========
    stock_pool: str = "all_a"  # all_a / hs300 / zz500 / etf_pool
    etf_pool: List[str] = field(default_factory=lambda: [
        '510300.XSHG',  # 沪深300ETF
        '510500.XSHG',  # 中证500ETF
        '159915.XSHE',  # 创业板ETF
        '510050.XSHG',  # 上证50ETF
    ])
    exclude_st: bool = True
    exclude_kcb: bool = True
    exclude_bj: bool = True
    new_stock_days: int = 250
    price_filter: Optional[float] = None  # 股价上限过滤
    
    # ========== 仓位配置 ==========
    stock_num: int = 10
    position_mode: str = "equal"  # equal / momentum_weight / epo
    max_positions: int = 10
    
    # ========== 调仓配置 ==========
    rebalance_frequency: str = "daily"  # daily / weekly / monthly
    rebalance_weekday: int = 1  # 1=周一
    rebalance_monthday: int = 1
    
    # ========== 风控配置 ==========
    use_stop_loss: bool = True
    stop_loss_pct: float = 0.05  # 5%止损
    use_trailing_stop: bool = False
    trailing_stop_pct: float = 0.05
    check_limit_up: bool = True  # 涨停股处理
```

### 3.3 框架主类
```python
class MomentumReversalStrategy:
    """动量/反转策略通用框架"""
    
    def __init__(self, config: MomentumReversalConfig):
        self.config = config
        self.momentum_calc = MomentumCalculator()
        self.reversal_calc = ReversalCalculator()
        self.combiner = MomentumReversalCombiner()
        self.hold_list = []
        self.yesterday_HL_list = []
    
    def initialize(self, context):
        """初始化函数 - 对应聚宽initialize"""
        cfg = self.config
        # 设置基准、交易成本等
        set_benchmark('000300.XSHG')
        set_option('use_real_price', True)
        set_option('avoid_future_data', True)
        
        # 根据调仓频率设置运行时间
        if cfg.rebalance_frequency == 'daily':
            run_daily(self.rebalance, time='9:30')
        elif cfg.rebalance_frequency == 'weekly':
            run_weekly(self.rebalance, weekday=cfg.rebalance_weekday, time='9:30')
        elif cfg.rebalance_frequency == 'monthly':
            run_monthly(self.rebalance, monthday=cfg.rebalance_monthday, time='9:30')
        
        # 涨停检查
        if cfg.check_limit_up:
            run_daily(self.check_limit_up, time='14:00')
    
    def rebalance(self, context):
        """核心调仓逻辑"""
        # 1. 获取股票池
        stock_list = self._get_stock_pool(context)
        
        # 2. 计算动量得分
        if self.config.momentum_type != 'none':
            momentum_scores = self._calculate_momentum(context, stock_list)
        
        # 3. 计算反转得分
        if self.config.reversal_type != 'none':
            reversal_scores = self._calculate_reversal(context, stock_list)
        
        # 4. 组合得分
        if self.config.use_combination:
            final_scores = self.combiner.combine_momentum_reversal(
                momentum_scores, reversal_scores, self.config.momentum_weight
            )
        elif self.config.momentum_type != 'none':
            final_scores = momentum_scores
        else:
            final_scores = reversal_scores
        
        # 5. 择时判断
        if self.config.use_timing:
            timing_signal = self._get_timing_signal(context)
            if timing_signal == 'SELL':
                self._close_all_positions(context)
                return
        
        # 6. 排序选股
        target_list = self._select_stocks(final_scores)
        
        # 7. 执行调仓
        self._execute_trade(context, target_list)
    
    def _get_stock_pool(self, context) -> List[str]:
        """获取股票池并过滤"""
        if self.config.stock_pool == 'etf_pool':
            return self.config.etf_pool
        
        initial_list = get_all_securities().index.tolist()
        initial_list = self._filter_st_stock(initial_list)
        initial_list = self._filter_new_stock(context, initial_list)
        initial_list = self._filter_paused_stock(initial_list)
        initial_list = self._filter_kcbj_stock(initial_list)
        return initial_list
    
    def _calculate_momentum(self, context, stock_list) -> pd.Series:
        """计算动量得分"""
        cfg = self.config
        if cfg.momentum_type == 'return':
            return self.momentum_calc.get_price_momentum(
                stock_list, context.previous_date, cfg.momentum_periods
            )
        elif cfg.momentum_type == 'slope':
            return self.momentum_calc.get_slope_momentum(
                stock_list, context.previous_date, cfg.momentum_period
            )
        elif cfg.momentum_type == 'bias':
            return self.momentum_calc.get_bias_momentum(
                stock_list, context.previous_date, cfg.bias_period, cfg.momentum_period
            )
    
    def _calculate_reversal(self, context, stock_list) -> pd.Series:
        """计算反转得分"""
        cfg = self.config
        if cfg.reversal_type == 'short_term':
            return self.reversal_calc.get_short_term_reversal(
                stock_list, context.previous_date, cfg.reversal_period
            )
        elif cfg.reversal_type == 'ma_break':
            return self.reversal_calc.get_ma_reversal(
                stock_list, context.previous_date, cfg.ma_break_period
            )
    
    def _get_timing_signal(self, context) -> str:
        """获取择时信号"""
        cfg = self.config
        if cfg.timing_method == 'rsrs_ma':
            rsrs_signal = self.combiner.get_rsrs_timing_signal(
                '000300.XSHG', context.previous_date,
                cfg.rsrs_N, cfg.rsrs_M, cfg.rsrs_threshold
            )
            ma_signal = self.combiner.get_ma_timing_signal(
                '000300.XSHG', context.previous_date,
                cfg.ma_short_period, cfg.ma_diff_period
            )
            if rsrs_signal == 'SELL' and ma_signal == 'SELL':
                return 'SELL'
            return 'BUY'
        elif cfg.timing_method == 'rsrs_only':
            return self.combiner.get_rsrs_timing_signal(
                '000300.XSHG', context.previous_date,
                cfg.rsrs_N, cfg.rsrs_M, cfg.rsrs_threshold
            )
    
    def _select_stocks(self, scores: pd.Series) -> List[str]:
        """根据得分选股"""
        ascending = (self.config.momentum_direction == 'desc')
        sorted_stocks = scores.sort_values(ascending=ascending)
        return sorted_stocks.index.tolist()[:self.config.stock_num]
    
    def _execute_trade(self, context, target_list: List[str]):
        """执行调仓交易"""
        # 卖出不在目标列表的
        for stock in list(context.portfolio.positions.keys()):
            if stock not in target_list and stock not in self.yesterday_HL_list:
                self._close_position(context, stock)
        
        # 买入目标股票
        position_count = len(context.portfolio.positions)
        target_num = len(target_list)
        if target_num > position_count:
            value = context.portfolio.cash / (target_num - position_count)
            for stock in target_list:
                if stock not in context.portfolio.positions:
                    self._open_position(context, stock, value)
                    if len(context.portfolio.positions) == target_num:
                        break
    
    # ===== 标准过滤函数 =====
    def _filter_paused_stock(self, stock_list):
        current_data = get_current_data()
        return [s for s in stock_list if not current_data[s].paused]
    
    def _filter_st_stock(self, stock_list):
        current_data = get_current_data()
        return [s for s in stock_list
                if not current_data[s].is_st
                and 'ST' not in current_data[s].name
                and '*' not in current_data[s].name
                and '退' not in current_data[s].name]
    
    def _filter_kcbj_stock(self, stock_list):
        return [s for s in stock_list 
                if not s.startswith('688')  # 科创板
                and not s.startswith('4')   # 北交所
                and not s.startswith('8')]  # 北交所
    
    def _filter_new_stock(self, context, stock_list):
        yesterday = context.previous_date
        return [s for s in stock_list 
                if yesterday - get_security_info(s).start_date >= datetime.timedelta(days=self.config.new_stock_days)]
    
    def _open_position(self, context, security, value):
        order = order_target_value(security, value)
        return order is not None and order.filled > 0
    
    def _close_position(self, context, security):
        order = order_target_value(security, 0)
        return order is not None and order.status == OrderStatus.held and order.filled == order.amount
    
    def _close_all_positions(self, context):
        for stock in list(context.portfolio.positions.keys()):
            self._close_position(context, stock)
    
    def check_limit_up(self, context):
        """检查涨停股"""
        if self.yesterday_HL_list:
            for stock in self.yesterday_HL_list:
                current_data = get_price(stock, end_date=context.current_dt, 
                                         frequency='1m', fields=['close', 'high_limit'],
                                         count=1, panel=False, fill_paused=True)
                if current_data.iloc[0, 0] < current_data.iloc[0, 1]:
                    self._close_position(context, stock)
```

---

## 四、典型策略映射示例

### 4.1 策略1: 简单价格动量 → `59 年化62%的动量策略`
```python
config = MomentumReversalConfig(
    momentum_type="return",
    momentum_period=91,
    stock_pool="all_a",
    stock_num=20,
    price_filter=5,  # 股价>5元
    rebalance_frequency="daily",
    use_timing=False,
    new_stock_days=250,
)
# 核心代码:
# momentum = (close[-1] - close[0]) / close[0]
# 选动量最大的N只
```

### 4.2 策略2: 斜率动量ETF轮动 → `05 8年10倍回撤小,有滑点,ETF动量简单轮动策略`
```python
config = MomentumReversalConfig(
    momentum_type="slope",
    momentum_period=29,
    stock_pool="etf_pool",
    etf_pool=['159915.XSHE', '510300.XSHG', '510500.XSHG'],
    stock_num=1,
    use_timing=True,
    timing_method="rsrs_ma",
    rsrs_N=18,
    rsrs_M=600,
    rsrs_threshold=0.7,
    rebalance_frequency="daily",
)
# 核心代码:
# score = annualized_returns * r_squared
# 选得分最高的ETF + RSRS择时
```

### 4.3 策略3: 四大搅屎棍行业动量 → `05 四大搅屎棍策略`
```python
config = MomentumReversalConfig(
    momentum_type="sector",
    momentum_period=20,
    stock_pool="all_a",
    stock_num=10,
    rebalance_frequency="weekly",
    use_timing=False,
)
# 核心逻辑:
# 1. 计算各行业站上MA20的股票占比
# 2. 如果"银行、有色、钢铁、煤炭"占比不在前列 → 开仓
# 3. 选股条件: ROE>0.15, ROA>0.10, 市值排序
```

### 4.4 策略4: 超跌反弹MA55突破 → `30 【超跌反弹v1.0】3年2.3倍的超跌反弹小牛股`
```python
config = MomentumReversalConfig(
    reversal_type="ma_break",
    ma_break_period=55,
    stock_pool="all_a",
    stock_num=5,
    use_timing=False,
    use_stop_loss=True,
    stop_loss_pct=0.05,
)
# 买入条件:
# 1. 近40天站上MA55天数 ≤ 3
# 2. 近3天有站上MA55
# 3. 近10天最低价是近60天最低
# 卖出条件:
# 1. 跌破MA5 / 下跌5% / 高点回撤5% / 总亏损5% / 跌破MA55
```

### 4.5 策略5: 动量反转双效应 → `67 基于动量和反转效应的沪深300成分股策略`
```python
config = MomentumReversalConfig(
    momentum_type="return",
    momentum_period=70,  # 14*5日
    reversal_type="short_term",
    reversal_period=30,  # 6*5日
    use_combination=True,
    momentum_weight=0.5,
    stock_pool="hs300",
    stock_num=25,  # 反转25只 + 动量30只
    rebalance_frequency="weekly",
)
# 核心逻辑:
# 反转: 选30日跌幅最大的25只
# 动量: 选70日涨幅最大的30只(分批建仓)
```

### 4.6 策略6: 大小盘动量切换 → `27 根据动量模型切换大小盘板块选股策略`
```python
config = MomentumReversalConfig(
    momentum_type="index_momentum",
    momentum_period=89,
    use_regime_switching=True,
    regime_method="index_momentum",
    stock_pool="all_a",
    stock_num=10,  # 大盘10只 / 小盘30只
    rebalance_frequency="monthly",
)
# 核心逻辑:
# 1. 计算上证50 vs 国证2000的斜率得分
# 2. 得分高的大盘 → 选大盘价值股(PE/PB/ROE/FCF)
# 3. 得分高的小盘 → 选小盘成长股(市值排序)
```

### 4.7 策略7: 行业反转效应 → `51 行业反转效应（年化32%，回撤8%）`
```python
config = MomentumReversalConfig(
    reversal_type="industry",
    stock_pool="industry_etf",
    stock_num=5,  # 5个行业
    rebalance_frequency="monthly",
)
# 核心逻辑:
# 1. 计算各行业rolling月份超额收益
# 2. 短期(2月)相关性 × 短期收益 = 短期预测
# 3. 中期(6月)相关性 × 中期收益 = 中期预测
# 4. 综合排序选前5个行业 → 买ETF或行业股票
```

### 4.8 策略8: 乖离率动量+RSRS择时 → `16 动量ETF轮动RSRS择时-升级`
```python
config = MomentumReversalConfig(
    momentum_type="bias",
    bias_period=90,
    momentum_period=10,
    stock_pool="etf_pool",
    stock_num=1,
    use_timing=True,
    timing_method="rsrs_only",
    rsrs_N=18,
    rsrs_M=1100,
    rsrs_threshold=0.7,
    rebalance_frequency="daily",
)
# 核心逻辑:
# bias = Price / MA90 - 1
# momentum = polyfit(bias序列, 1).slope
# 选乖离动量最强的ETF + RSRS择时
```

---

## 五、策略扩展指南

### 5.1 新增动量计算方法
```python
# 在MomentumCalculator中添加新方法
def get_cross_sectional_momentum(self, stock_list, date, period=20):
    """横截面动量(相对强弱)"""
    # 计算每只股票相对基准的超额收益
    pass

def get_time_series_momentum(self, stock_list, date, periods=[12, 1]):
    """时序动量(12-1动量)"""
    # 过去12个月收益 - 过去1个月收益
    pass
```

### 5.2 新增反转信号
```python
# 在ReversalCalculator中添加新方法
def get_volume_reversal(self, stock_list, date, period=5):
    """量价反转(放量下跌后反弹)"""
    pass

def get_sentiment_reversal(self, stock_list, date):
    """情绪反转(恐慌指数/换手率极端值)"""
    pass
```

### 5.3 新增择时方法
```python
# 在MomentumReversalCombiner中添加新方法
def get_breadth_timing(self, index, date, threshold=0.5):
    """市场宽度择时(创新高股票占比)"""
    pass

def get_volatility_timing(self, date, period=20):
    """波动率择时(低波动买入,高波动观望)"""
    pass
```

### 5.4 风控扩展
```python
# 在MomentumReversalStrategy中添加风控模块
def apply_portfolio_risk_control(self, context):
    """组合级别风控"""
    # 1. 最大回撤控制
    # 2. 行业集中度限制
    # 3. 单只股票仓位上限
    # 4. 波动率目标仓位调整
    pass
```

### 5.5 常见陷阱与规避
| 陷阱 | 表现 | 规避方法 |
|-----|------|---------|
| 未来函数 | 使用当天收盘价选股 | end_date=context.previous_date |
| 停牌风险 | 买入后停牌无法卖出 | filter_paused_stock + 检查 |
| 涨跌停无法成交 | 涨停买不到/跌停卖不出 | filter_limitup/limitdown |
| 次新股波动大 | 上市初期价格不稳定 | filter_new_stock(250天) |
| 动量崩溃 | 趋势突然反转导致大亏 | RSRS择时 + 止损 |
| 过度拟合 | 参数过多、回测完美 | 样本外测试、参数稳健性 |

---

## 六、完整文件索引

### 动量策略文件
| 序号 | 文件名 | 策略类型 | 核心动量方法 |
|-----|-------|---------|-------------|
| 1 | `59 年化62%的动量策略.txt` | 个股动量 | 91日价格收益率 |
| 2 | `49 动量策略年化62%.txt` | 个股动量 | 91日价格收益率 |
| 3 | `05 8年10倍回撤小,有滑点,ETF动量简单轮动策略.txt` | ETF动量 | 斜率×判定系数 |
| 4 | `88 基于动量因子的ETF轮动加上RSRS择时.txt` | ETF动量 | 斜率×判定系数 |
| 5 | `16 动量ETF轮动RSRS择时-升级.txt` | ETF动量 | 乖离率动量 |
| 6 | `69 宽基BBI动量追涨完美避开这波大跌.txt` | ETF动量 | BBI动量 |
| 7 | `81 搞市场最靓的仔！指数ETF动量轮动策略-2.txt` | ETF动量 | 21日涨幅 |
| 8 | `17 多品种ETF动量轮动+EPO优化.txt` | ETF动量 | 斜率×判定系数+EPO |
| 9 | `27 根据动量模型切换大小盘板块选股策略.txt` | 指数动量 | 斜率×判定系数 |
| 10 | `30 价值成长轮动策略.txt` | 指数动量 | 斜率×判定系数 |
| 11 | `74 【动量应用V1】12年120倍，根据研报思路调整.txt` | 因子动量 | MAC120/MAC20 |
| 12 | `36 最简强者恒强策略.txt` | 行业动量 | 行业内市值排序 |

### 反转策略文件
| 序号 | 文件名 | 策略类型 | 核心反转方法 |
|-----|-------|---------|-------------|
| 1 | `30 【超跌反弹v1.0】3年2.3倍的超跌反弹小牛股.txt` | 超跌反弹 | MA55突破 |
| 2 | `93 【超跌反弹v1.0】3年2.3倍的超跌反弹小牛股.txt` | 超跌反弹 | MA55突破 |
| 3 | `71 回踩均线搏反弹，21年来70%..txt` | 均线回踩 | MA21/55/120多头回踩 |
| 4 | `74 迪马克TD趋势反转指标.txt` | TD反转 | DeMark Setup/Count |
| 5 | `51 行业反转效应（年化32%，回撤8%）.txt` | 行业反转 | 超额收益相关性 |
| 6 | `69 人工智能早晨十字星模型（反转形态预测）.txt` | K线反转 | 十字星形态 |

### 动量反转组合策略文件
| 序号 | 文件名 | 策略类型 | 组合方式 |
|-----|-------|---------|---------|
| 1 | `67 基于动量和反转效应的沪深300成分股策略.txt` | 双效应 | 30日反转+70日动量 |
| 2 | `39 干积分-大小盘反复横跳V2.0.txt` | 风格轮动 | 20日指数动量切换 |
| 3 | `45 此消彼长.txt` | 多因子 | 大小盘组合 |
| 4 | `05 四大搅屎棍策略.txt` | 行业动量 | 行业宽度+基本面 |
| 5 | `51 "四大搅屎棍策略"学习笔记-有魔改.txt` | 行业动量 | 行业宽度+基本面 |
| 6 | `61 简单ETF策略，年化97%.txt` | 均线趋势 | MA60向上+价格<MA60 |

### 相关研究笔记
| 序号 | 文件名 | 类型 |
|-----|-------|------|
| 1 | `75 方正多因子选股系列研究之二 小镇做题家 潮汐因子构建.ipynb` | 潮汐因子 |
| 2 | `75 方正多因子选股系列研究之三 小镇做题家 勇攀高峰因子.ipynb` | 勇攀高峰因子 |
| 3 | `81 方正多因子选股系列研究之四 小镇做题家版本：个股动量效应识别及球队硬币因子构建.ipynb` | 个股动量效应 |
| 4 | `91 【复现】基于深度多任务学习时序动量组合策略.ipynb` | 时序动量 |
| 5 | `66 研究 【复现】A股日内动量效应（一）半小时 涨跌幅间的规律.ipynb` | 日内动量 |
| 6 | `43 轮动ETF策略中的动量因子分析.ipynb` | 动量因子分析 |
