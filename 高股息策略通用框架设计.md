# 高股息策略通用框架设计

## 一、策略类型概述

### 核心逻辑
高股息策略的核心是**"分红即现金流"**，通过筛选持续稳定分红、股息率较高的股票，获取：
1. **确定性收益**：现金分红提供基础回报
2. **防御属性**：高分红企业通常盈利稳定、现金流充裕
3. **估值安全边际**：高股息率往往对应低估值

### 适用场景
- 震荡市/熊市防御
- 长期价值投资
- 低利率环境下的收益增强

### 风险特征
- 回撤普遍较低（多数策略回撤<30%）
- 年化收益区间：29%~100%+（取决于附加因子）
- "分红陷阱"风险：需配合质量因子过滤

---

## 二、子策略变体分类

| 编号 | 策略名称 | 核心特征 | 年化/回撤 |
|------|---------|---------|----------|
| V1 | **纯红利搬砖** | 仅按股息率排序，单股/月度调仓 | 29% |
| V2 | **高股息+低杠杆+小市值** | 股息率前50% → 低MLEV → 小流通市值 | ~100% |
| V3 | **高股息+低PEG+低价** | 股息率前25% → PEG过滤 → 低价股 | 高收益 |
| V4 | **高股息+低PE+高增长** | 3年股息率 → PE<25 → 营收/利润增长 | 稳健型 |
| V5 | **高股息+行业中性** | 5年连续分红 → 行业分散(每行业2只) → 股息率>3% | 分散型 |
| V6 | **高股息+ROIC+应付账款周转** | 股息率前20% → ROIC>8% → 应付账款周转天数 | 质量型 |
| V7 | **高股息+PB-ROE+小市值** | 股息率前25% → PEG<0 → PB<3 → 小市值 | 10年206倍 |
| V8 | **国九条红利因子** | 新规分红要求(3年>5000万或>30%净利润) → 审计意见过滤 | 103%/24% |
| V9 | **红利三因子轮动** | 高分红60% + 高PE60% + 高增长60% → 取并集按市值排序 | 60%/19% |
| V10 | **大市值高股息** | 中证全指 → PB<1 → 现金流>0 → 股息率前50% | 稳健大盘 |

---

## 三、通用代码框架

### 3.1 核心模块定义

#### 模块1: 分红数据获取器 (DividendDataProvider)

```python
class DividendDataProvider:
    """
    所有高股息策略中，分红数据获取代码几乎100%一致
    核心表: finance.STK_XR_XD (除权除息表)
    核心字段: code, a_registration_date, bonus_amount_rmb
    """
    
    def get_dividend_data(self, stock_list, context, years=1):
        """
        获取历史分红数据 - 所有策略的通用模板
        注意: finance.run_query 最多返回4000行，需分批查询
        """
        time1 = context.previous_date
        time0 = time1 - datetime.timedelta(days=365 * years)
        
        interval = 1000
        list_len = len(stock_list)
        
        q = query(
            finance.STK_XR_XD.code,
            finance.STK_XR_XD.a_registration_date,
            finance.STK_XR_XD.bonus_amount_rmb
        ).filter(
            finance.STK_XR_XD.a_registration_date >= time0,
            finance.STK_XR_XD.a_registration_date <= time1,
            finance.STK_XR_XD.code.in_(stock_list[:min(list_len, interval)])
        )
        df = finance.run_query(q)
        
        if list_len > interval:
            df_num = list_len // interval
            for i in range(df_num):
                q = query(
                    finance.STK_XR_XD.code,
                    finance.STK_XR_XD.a_registration_date,
                    finance.STK_XR_XD.bonus_amount_rmb
                ).filter(
                    finance.STK_XR_XD.a_registration_date >= time0,
                    finance.STK_XR_XD.a_registration_date <= time1,
                    finance.STK_XR_XD.code.in_(
                        stock_list[interval*(i+1):min(list_len, interval*(i+2))]
                    )
                )
                temp_df = finance.run_query(q)
                df = df.append(temp_df)
        
        return df
    
    def calc_dividend_ratio(self, df_dividend, date):
        """
        计算股息率 = 累计分红金额(万元)/10000 / 总市值(亿元)
        """
        dividend = df_dividend.fillna(0)
        dividend = dividend.set_index('code')
        dividend = dividend.groupby('code').sum()
        
        temp_list = list(dividend.index)
        
        q = query(valuation.code, valuation.market_cap).filter(
            valuation.code.in_(temp_list)
        )
        cap = get_fundamentals(q, date=date)
        cap = cap.set_index('code')
        
        DR = pd.concat([dividend, cap], axis=1, sort=False)
        DR['dividend_ratio'] = (DR['bonus_amount_rmb'] / 10000) / DR['market_cap']
        
        return DR
    
    def get_consecutive_dividend_years(self, stock_list, context, min_years=5):
        """获取连续分红年数"""
        curr_DR = pd.DataFrame()
        for i in range(min_years):
            time1 = context.previous_date if i == 0 else time0
            time0 = time1 - datetime.timedelta(days=365)
            DR = self.get_dividend_data(stock_list, context, years=1)
            stock_list = list(DR.index)
            if i == 0:
                curr_DR = DR
        return curr_DR.reindex(stock_list)
```

#### 模块2: 股息率筛选器 (DividendYieldScreener)

```python
class DividendYieldScreener:
    def screen_by_percentile(self, DR_df, sort=True, p1=0, p2=0.25):
        """按股息率分位数筛选"""
        DR = DR_df.sort_values(by=['dividend_ratio'], ascending=sort)
        final_list = list(DR.index)[int(p1*len(DR)):int(p2*len(DR))]
        return final_list
    
    def screen_by_threshold(self, DR_df, min_yield=0.03):
        """按绝对股息率阈值筛选"""
        return list(DR[DR['dividend_ratio'] > min_yield].index)
    
    def screen_consecutive(self, context, stock_list, years=5):
        """连续N年均有分红的股票"""
        provider = DividendDataProvider()
        final_DR = provider.get_consecutive_dividend_years(stock_list, context, years)
        return final_DR
```

#### 模块3: 红利质量筛选器 (DividendQualityScreener)

```python
class DividendQualityScreener:
    def screen_by_new_regulation(self, stock_list, context):
        """国九条新规筛选"""
        pass
    
    def screen_by_audit_opinion(self, stock_list, context, years=3):
        """审计意见过滤"""
        expection_list = [3, 4, 5, 7]
        final_list = []
        for stock in stock_list:
            q = query(finance.STK_AUDIT_OPINION).filter(
                finance.STK_AUDIT_OPINION.code == stock
            )
            df = finance.run_query(q)
            if not df['opinion_type_id'].isin(expection_list).any():
                final_list.append(stock)
        return final_list
```

### 3.2 框架配置类

```python
@dataclass
class HighDividendConfig:
    # 股票池配置
    stock_pool: str = "all_a"
    exclude_st: bool = True
    exclude_kcbj: bool = True
    new_stock_days: int = 375
    
    # 股息率条件
    dividend_years: int = 1
    min_dividend_yield: float = 0.0
    yield_percentile_top: float = 0.25
    yield_percentile_bottom: float = 0.0
    
    # 质量条件
    use_pe_filter: bool = False
    pe_min: float = 0
    pe_max: float = 25
    use_peg_filter: bool = False
    use_roe_filter: bool = False
    use_revenue_growth: bool = False
    use_profit_growth: bool = False
    use_new_regulation: bool = False
    
    # 附加因子
    use_low_leverage: bool = False
    use_high_volatility: bool = False
    use_small_cap: bool = False
    use_low_price: bool = False
    use_industry_neutral: bool = False
    
    # 仓位配置
    stock_num: int = 10
    position_mode: str = "equal"
    
    # 调仓配置
    rebalance_frequency: str = "weekly"
    rebalance_day: int = 1
    
    # 止损止盈
    use_stop_loss: bool = False
    stop_loss_pct: float = 0.07
    
    # 特殊月份
    pass_april: bool = False
    etf_for_holiday: str = "511880.XSHG"
```

### 3.3 框架主类

```python
class HighDividendStrategy:
    def __init__(self, config: HighDividendConfig):
        self.config = config
        self.dividend_provider = DividendDataProvider()
        self.yield_screener = DividendYieldScreener()
        self.quality_screener = DividendQualityScreener()
        
    def initialize(self, context):
        set_benchmark('000300.XSHG')
        set_option('use_real_price', True)
        set_option('avoid_future_data', True)
        
        if self.config.rebalance_frequency == 'weekly':
            run_weekly(self.rebalance, self.config.rebalance_day, '09:30')
        elif self.config.rebalance_frequency == 'monthly':
            run_monthly(self.rebalance, self.config.rebalance_day, '09:30')
        
        run_daily(self.prepare_stock_list, '9:05')
        run_daily(self.check_limit_up, '14:00')
    
    def rebalance(self, context):
        # 1. 获取股票池
        stocks = self.get_initial_pool(context)
        # 2. 基础过滤
        stocks = self.apply_basic_filters(context, stocks)
        # 3. 股息率筛选
        stocks = self.screen_by_dividend(context, stocks)
        # 4. 质量筛选
        stocks = self.screen_by_quality(context, stocks)
        # 5. 附加因子
        stocks = self.screen_by_factors(context, stocks)
        # 6. 排序选股
        target_list = self.rank_and_select(context, stocks)
        # 7. 执行调仓
        self._execute_trade(context, target_list)
```

---

## 四、典型策略映射示例

### 策略1: 红利搬砖 (04 红利搬砖，年化29%)
```python
config = HighDividendConfig(
    stock_pool="000300.XSHG",
    dividend_years=3,
    yield_percentile_top=1.0,
    stock_num=1,
    rebalance_frequency="monthly",
    rebalance_day=1,
)
```

### 策略2: 高股息低杠杆小市值 (15/06)
```python
config = HighDividendConfig(
    stock_pool="all_a",
    dividend_years=1,
    yield_percentile_top=0.50,
    use_low_leverage=True,
    use_high_volatility=True,
    use_small_cap=True,
    stock_num=10,
    rebalance_frequency="weekly",
)
```

### 策略3: 菜场大妈股息率小市值 (35/36)
```python
config = HighDividendConfig(
    stock_pool="all_a",
    dividend_years=1,
    yield_percentile_top=0.25,
    use_peg_filter=True,
    use_low_price=True,
    use_small_cap=True,
    stock_num=5,
    pass_april=True,
    rebalance_frequency="monthly",
)
```

### 策略4: 国九条红利因子 (79/42)
```python
config = HighDividendConfig(
    stock_pool="399101.XSHE",
    use_new_regulation=True,
    use_audit_opinion=True,
    dividend_years=1,
    stock_num=4,
    pass_april=True,
    etf_for_holiday="511880.XSHG",
    use_stop_loss=True,
    stop_loss_pct=0.09,
    rebalance_frequency="weekly",
)
```

### 策略5: 高股息行业中性 (40)
```python
config = HighDividendConfig(
    stock_pool="all_a",
    dividend_years=5,
    min_dividend_yield=0.03,
    use_industry_neutral=True,
    max_per_industry=2,
    stock_num=10,
    rebalance_frequency="weekly",
)
```

---

## 五、完整文件索引

| 文件名 | 策略类型 | 股息率年数 | 筛选比例 | 持仓数 | 调仓频率 |
|--------|---------|-----------|---------|-------|---------|
| 04 红利搬砖 | 纯红利 | 3年 | 全部 | 1 | 月度 |
| 15 高股息低杠杆小市值 | 红利+因子 | 1年 | 前50% | 10 | 周度 |
| 06 高股息低杠杆稳定 | 红利+因子 | 1年 | 前50% | 10 | 周度 |
| 26 高股息率-低PEG-低价 | 红利+估值 | 1年 | 前25% | 20 | 月度 |
| 19 高股息低PE稳健增长 | 红利+成长 | 3年 | 前10% | 10 | 月度 |
| 04 高股息低PE高增长 | 红利+成长 | 3年 | 前10% | 10 | 月度 |
| 40 高股息策略 | 红利+行业 | 5年连续 | 阈值3% | 10 | 周度 |
| 60 高股息V1.0 | 红利+质量 | 3年 | 前20% | 5 | 月度 |
| 35/36 菜场大妈 | 红利+小市值 | 1年 | 前25% | 5 | 月度 |
| 79 国九条红利因子 | 红利+新规 | 1年 | 动态 | 4 | 周度 |
| 42 国九条三年分红 | 红利+新规 | 1年 | 前80% | 10 | 周度 |
| 87 山寨红利 | 纯红利 | 3年 | 前10% | 30 | 月度 |
| 77 研报三因子 | 红利+轮动 | 1年 | 前10% | 10 | 周度 |
| 70 股息率+均线 | 红利+技术 | 3年 | 多级筛选 | 5 | 月度 |
| 67 大市值价投高股息 | 红利+大盘 | 3年 | 前50% | - | 月度 |
